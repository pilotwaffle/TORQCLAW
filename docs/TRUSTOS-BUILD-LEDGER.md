# TORQCLAW TrustOS — Build Ledger

Program: PRD-TCLAW-TRUSTOS-001 rev 1.2 · Orchestrator: Fable 5 (G1D)
Role mapping note: G1R and G2A run as independent Opus instances; GLM-5.2 (RB) unavailable in this harness — substitutions are logged per ticket.

## Ticket log

### TCLAW-0A (stage v0) — CI pipeline: TypeScript core

- date: 2026-07-08
- branch: master @ c206d7a (committed: 0251bf4 docs, c206d7a ci; pushed to origin 2026-07-08)
- ticket: TCLAW-0A stage v0 (PRD §9.0.2)
- what changed: new file `.github/workflows/ci.yml` — push-to-master + PR triggers, ubuntu-latest, timeout 15m, checkout (no submodules), pnpm via packageManager field, Node 22 (required by --experimental-strip-types in contracts build), steps: install --frozen-lockfile → contracts build → typecheck → test. No other files touched; turbo.json ruled not-required (root test script is direct vitest, documented deviation from PRD line "add test to turbo pipeline").
- tests/checks run: local `pnpm install --frozen-lockfile` PASS, contracts build PASS (3 schemas × 2 dirs), `pnpm typecheck` PASS (12/12), `pnpm test` PASS (94/94); G2A independently re-ran contracts build + tests, 94/94 confirmed.
- G1R: not required (infra-only, no security/routing/approval/cost/contract-semantics surface).
- G2A result: PASS. Informational notes: (1) turbo test-task deviation should be mentioned in PR text; (2) consider concurrency + explicit permissions block at CI v1.
- github verification: CI run 28988129021 on c206d7a GREEN (all steps passed). Runner annotation: actions @v4 majors target deprecated Node 20 - bump action versions at CI v1.
- next ticket: TCLAW-10A (contracts:check drift gate + ConnectFrame emission + golden fixtures), then CI v1 wiring back into TCLAW-0A.
- blockers: none. TICKET COMPLETE AND ACCEPTED. Operator approved commit+push 2026-07-08.

### TCLAW-10A — contracts drift gate, ConnectFrame emission, golden fixtures

- date: 2026-07-08
- branch: master @ dcce64d (committed: 92e8515 feat, eedcc28 chore, dcce64d docs; pushed 2026-07-08; CI run 28990952999 GREEN; all 8 schema artifacts i/lf w/lf)
- ticket: TCLAW-10A (PRD §9.10.3, Epic 10 scope pulled into Phase 0)
- what changed: emit-schemas.ts emits ConnectFrame as 4th artifact (4 schemas × 2 dirs); new check-schemas.ts drift gate (re-emit to OS temp dir, file-set assertion + parsed-JSON deep-equality against both dirs, exit 1 on drift); scripts wired as contracts `check` + root `contracts:check`; 9 golden fixtures in tests/fixtures/ (6 ClientCommand variants + GatewayRequest + GatewayEvent + ConnectFrame) with vitest validation test; .gitignore un-ignores both generated schema dirs (G1R Option A ruling); new .gitattributes pins schema/fixture JSON to eol=lf, schema dirs linguist-generated; PRD §9.10.3 wording amended byte-diff → semantic (parsed-JSON) diff.
- design rulings: G1R APPROVE-WITH-CHANGES (CRLF hazard → deep-JSON compare + .gitattributes; 9 fixtures not 4; file-set assertion; temp-dir emission). Mid-ticket G1R ruling Option A: track both generated dirs — rationale: CI gate is a tautology against ignored dirs; turbo package-relative outputs cannot cover the cross-package Python schema write (cache-hit staleness hole); contracts.py self-containment promise for GPU-box deploys requires schemas present without a Node build.
- tests/checks run: contracts build (4×2) PASS; contracts:check exit 0 clean / exit 1 on three builder tamper tests + one independent G2A tamper (nested enum, single-dir) with precise JSON path reported; determinism verified (double-build md5 match); pnpm test 103/103 (94 pre-existing + 9 fixtures); typecheck 12/12.
- G2A result: PASS-WITH-NOTES. Note remediated: connect-frame fixture token was secret-shaped in a public repo → changed to FIXTURE-NOT-A-REAL-TOKEN; re-verified 103/103 + check exit 0. Residual: two short synthetic row IDs in fixtures assessed non-credential by G2A, Builder, and G1D.
- deferred notes: consider pinning zod exactly (caret bump can legitimately change emitted schemas — gate will catch it, regenerate+commit is the response); CI wiring of contracts:check is next ticket (TCLAW-0A v1) along with action-major bumps for the Node 20 runner deprecation.
- next ticket: TCLAW-0A v1 (wire contracts:check + six stub-mode e2e scripts into ci.yml, bump action majors).
- blockers: none. TICKET COMPLETE AND ACCEPTED. Operator approved 3-commit plan 2026-07-08.

### TCLAW-0A (stage v1) — CI integration: drift gate + stub-mode e2e

- date: 2026-07-08
- branch: master @ 38e6a38 (committed: 2d65635 ci, 9d2f083 docs, 38e6a38 fixup; CI run 28994626483 GREEN in 1m39s - all 18 steps incl. drift gate + six e2e in structural stub mode on Linux)
- ticket: TCLAW-0A stage v1 (PRD §9.0.2)
- what changed: ci.yml only. Added `pnpm contracts:check` after contracts build; `concurrency` (cancel-in-progress) + `permissions: contents: read`; action majors bumped (checkout v4→v7, setup-node v4→v6, pnpm/action-setup v4→v6, astral-sh/setup-uv v8 added) clearing the Node-20 runner deprecation; `uv sync` in engines/hermes_kernel (no submodule init — stub mode verified to require only mcp+jsonschema); `pnpm build` before e2e (Builder catch: e2e spawns gateway/channel-http dist/server.js which v0 never built); six named e2e steps.
- tests/checks run: Builder ran all six e2e locally exit 0 (one env-only retry: stale ports from a prior dev session), contracts:check exit 0, typecheck 12/12, vitest 103/103. G2A independently ran full `pnpm build` on cleared turbo cache (exit 0, 23.7s, console Next build succeeds with zero env vars — all env reads have ?? fallbacks), timed representative e2e (~10-12s each), YAML-validated the workflow, verified stub-mode guard at hermes_runner.py vendor-exists check.
- G1R: not required (infra-only; same rationale as v0).
- G2A result: PASS. Deferred INFO notes: (1) six e2e steps share ~/.torqclaw/state.db on the runner — benign today (assertions self-scoped per WS session) but set TORQCLAW_DATA_DIR per step if a future e2e asserts absolute DB state; (2) local stub path was HERMES_MODEL-unset, CI exercises missing-vendor path — both converge on the same guard.
- fixup: first run 28994268063 failed at job setup - astral-sh/setup-uv publishes exact tags only, no floating v8 major ref. Pinned v8.3.2; other three action refs verified to resolve as bare majors. Lesson recorded: verify ref RESOLUTION (git matching-refs), not release existence.
- next ticket: TCLAW-0B (role-based command authorization + channel task ownership) — G1R review MANDATORY (security surface).
- blockers: none. TICKET COMPLETE AND ACCEPTED (v0+v1). CI v2 deferred until TCLAW-0E delivers the Python test suite.

### TCLAW-0B — role-based command authorization + channel task ownership

- date: 2026-07-09
- branch: master (worktree changes complete, pending operator commit approval)
- ticket: TCLAW-0B (PRD §9.0.2) — the PRD's #1 security fix; G1R review MANDATORY, completed
- what changed (4 code files): NEW packages/gateway/src/authz.ts — pure authorize(role,cmd,ctx) allow-list with default-deny + checkResumeRole() guard; sessions.ts resolve() now returns persisted role (SELECT id,role) in both create/resume branches; server.ts — single default-deny authz gate before the dispatch switch (returns on deny), resume-role-mismatch guard closes socket 4003 before any command, denials via ephemeral sendErr + app.log.warn (not persisted), inline lookupTaskSession(SELECT session_id FROM tasks WHERE request_id=?); NEW tests/authz.test.ts. No contract/schema/dispatch/cancellations/channel-http changes; no migration.
- policy: operator=full; channel=SUBMIT_PROMPT + MEMORY SHOW + CANCEL_TASK(own session only, not-found denies) + all else deny; node=deny-all incl SUBMIT_PROMPT. Unmapped/future actions default-deny for non-operator.
- vulns closed (both G1R-confirmed pre-existing, beyond PRD text): (1) CANCEL_TASK was unauthenticated cross-session — fired cancellations.request() for any UUID with no ownership check; (2) resume path never validated role — a client could hijack any session id or lie about role in a resume frame, bypassing the whole scheme. Guard rejects both directions (operator↔channel) with 4003.
- design rulings: G1R APPROVE-WITH-CHANGES — required single pre-switch default-deny gate (not per-case), resolve() role plumbing, 4003 reject on mismatch (never mint fresh session), CANCEL ownership via tasks.session_id, ephemeral denials, documented residuals.
- tests/checks run: typecheck 12/12; pnpm test 146/146 (103 baseline + 43 across authz suite incl. role×action matrix, CANCEL ownership own/other/unknown, node deny-all, default-deny unmapped, reason-no-leak, checkResumeRole all ordered pairs + fresh-session inertness, real-sqlite integration proving both hijack directions rejected); contracts:check exit 0; pnpm build 7/7; e2e-cancel exit 0 (operator cancel intact), e2e-channel exit 0 (channel seat SUBMIT_PROMPT through new gate).
- G2A result: PASS-WITH-NOTES on first pass — all 6 core security properties verified closed with file:line evidence; sole finding was ZERO coverage on the resume-mismatch 4003 guard. G1D did not accept an untested load-bearing security guard: sent back to Builder, who extracted checkResumeRole() as a pure helper called verbatim by server.ts and added exhaustive tests (production path == tested unit). Gap closed; re-verified 146/146 + both e2e green.
- build incident: Builder subagent was killed mid-amendment by an account monthly spend limit, but had already completed the helper extraction, server.ts wiring, and tests before termination (its dying message understated progress). G1D verified the tree directly (compiles, 146 tests, e2e green, no parallel untested copy) rather than trusting the interrupted report. NOTE: spend limit is live on the Fable 5 model — may constrain further subagent dispatch.
- residuals (documented, out of scope for 0B): shared gateway token still lets a token holder CREATE an operator session (0B closes resume-hijack + per-command escalation, not session creation); a permitted channel seat can still burn unbounded FRONTIER budget and submit unbounded prompt volume — per-channel budget clamps + rate limiting tracked in Epic 7; channel-http shared-session callers within one front-door token are one trust domain (per-caller isolation deferred until per-caller channel identity exists).
- next ticket: TCLAW-0C (behavior-based capability classification, fail-closed unknown default, path-scope write-check fix). G1R review recommended (touches approval/path-scope security surface).
- blockers: operator commit approval pending. Account spend limit may block further work — operator action may be required (/usage-credits or model switch).

### TCLAW-0C — behavior-based capability classification, fail-closed, path-scope fix

- date: 2026-07-09
- branch: ticket/tclaw-0c-capability-classifier -> PR #1 (base master). Commits: 12705da (wip: classifier + path-scope fix), d429ffd (remove name->read inference, fail-closed), 86d2784 + c5ff9e3 (OS-portable test fixtures). NOT merged — awaiting operator approval.
- ticket: TCLAW-0C (PRD §9.0.2). Security-sensitive; G1R + G2A mandatory.
- what changed (4 files): NEW packages/bridge/src/capability.ts (pure classifyCapability + isWriteClass + scopeModeFor); registry.ts (capability field, path-scope mode = scopeModeFor(capability) — the bug fix, decoupled from requiresApproval; requiresApproval = isWriteClass(cap) || approvalPatterns); serverConfig.ts (capabilities annotation map); tests/capability.test.ts + tests/registry-scope.test.ts (NEW); ops/servers.example.json (annotation remedy example).
- FINAL DESIGN (after 2 rejected attempts): NO path from a bare tool name to 'read'. read reachable only via P1 explicit config annotation or P3 MCP readOnlyHint. Name patterns classify write/exec/send by token-bounded verb match; everything else fails closed to write. Satisfies PRD "unknown never means read". Honest read tools fail closed to gated write-scope until annotated (PRD-intended; remedy = capabilities map in servers.json).
- design rulings: G1R APPROVE-WITH-CHANGES (decision table: config->MCP->name-pattern->fail-closed; preserved-default guarantee; read allowlist to avoid mass regression). NOTE: the read-allowlist that G1R+G1D initially specified was itself the defect — see audit history.
- ADVERSARIAL AUDIT HISTORY (3 rounds, multi-agent workflows): v1 (prefix read-allowlist) — single-agent G2A passed PASS-WITH-NOTES, but a parallel 4-skeptic workflow found it leaked 196 dangerous names -> REJECTED. v2 (whole-name read grammar) — leaked 62 honest-dangerous names (get_all_secrets, read_private_key) AND over-gated 100% of 53 real MCP read tools incl. the repo's entire TradingView surface, plus a P4 substring bug (get_deployment->write) -> REJECTED. v3 (no name->read path, PRD-faithful) — 3-lens confirmation workflow: no-name-read-path PROVEN structurally + empirically (266-name corpus, 0 leaks, non-vacuous controls); PRD-conformant; scope clean -> PASS-WITH-NOTES, merge-approved. Lesson: multi-agent adversarial verification caught what single-pass G2A and 2 builder rounds shipped past.
- hardening added: frozen 33-name adversarial regression-guard corpus in tests/capability.test.ts locks the no-name-read-path invariant in CI (with non-vacuous P1/P3 controls + non-ASCII fixture guards) so it cannot silently regress.
- CI incident: first two PR CI runs failed on tests/registry-scope.test.ts — hardcoded C:\ fixture paths resolved relative to cwd on Linux, flipping in-scope vs outside. First fixup was incomplete (search-replace missed 6 literals; local Windows run passed anyway, masking it). Second fixup made all fixtures platform-anchored; verified in-scope/outside resolution correct under both win32 and posix semantics before pushing. LESSON: a green local run on Windows does NOT prove OS-portability; verify POSIX path semantics explicitly.
- tests/checks: typecheck 12/12; pnpm test 266/266; contracts:check 0; build 7/7; both approval e2e exit 0. CI run 29022942408 GREEN in 1m42s (full v1 pipeline incl. 6 e2e on Linux).
- G2A result: PASS-WITH-NOTES (merge-approved). Residuals documented: (1) intentional over-gating + annotation remedy is the headline operator note; (2) example annotation lists tools beyond the allowlist (clarified in servers.example.json comment); (3) regression-guard corpus added.
- next ticket: TCLAW-0D/0E/0F (cost-breaker unit tests, Python test suite, token hygiene) then CI v2. TCLAW-0D/0F low-risk; 0E enables CI v2.
- blockers: operator merge approval for PR #1.

### TCLAW-0D — cost-breaker unit tests

- date: 2026-07-09
- branch: ticket/tclaw-0d-cost-breaker-tests @ e3d3a69 -> PR (base master). NOT merged — awaiting operator approval.
- ticket: TCLAW-0D (PRD §9.0.2). Conservation mode (self-implemented, no builder subagent) — but touches cost-enforcement, so independent Opus G2A was mandatory and NOT skipped.
- what changed (4 files, minimal seams): dispatch.ts resolveBudget made `export` (one word, no logic change); hermes.ts circuit-breaker decision extracted from the poll loop into a pure exported evaluateSpend(costUsd,budget,state,now,intervalMs) + HeartbeatState (loop calls it verbatim — production path == tested unit, no parallel copy); NEW tests/budget.test.ts (8) + tests/circuit-breaker.test.ts (15).
- coverage: resolveBudget precedence (maxCost -> env default -> unlimited) incl. maxCost:0 honored as real budget, 0/negative/non-numeric/empty env ignored; breaker trip on strict over-budget, no-trip at/under/unlimited, $0-budget trips on any spend; unreportable spend (null/undefined/NaN/string) skipped with NO fabricated zero and state untouched; heartbeat cadence (once per interval, only on cost change, injected clock — no timer flake); /budget/i regression guard tied to e2e-budget + dispatch BUDGET mapping; CircuitBreakerError shape.
- behavior-preserving: message strings unchanged (Budget exceeded / Spend so far); e2e-budget exit 0 (BUDGET terminal ERROR fired through refactored loop); both FRONTIER approval e2e + one-shot e2e exit 0.
- tests/checks: typecheck 12/12; pnpm test 289/289 (+23); contracts:check 0; build 7/7; e2e-budget + e2e-approval-cloud + e2e exit 0.
- G2A result (independent Opus 4.8): PASS — merge-ready. Reconstructed old-vs-new logic as a truth table over {number,null,undefined,NaN,string} x {budget undefined,0,1} x interval x cost-change; confirmed semantically identical on every cell; no parallel copy; teeth-check (flip > to >=) broke exactly the boundary test, proving non-vacuous. 2 info notes (NaN heartbeat cosmetics pre-existing/preserved; test-seam widening acceptable) — non-blocking.
- next ticket: TCLAW-0F (token hygiene) then TCLAW-0E (Python test suite, gates CI v2).
- blockers: operator merge approval.

### TCLAW-0F — token hygiene

- date: 2026-07-09
- branch: ticket/tclaw-0f-token-hygiene @ a4a7b52 -> PR (base master). NOT merged — awaiting operator approval.
- ticket: TCLAW-0F (PRD §9.0.2). Conservation mode (self-implemented); touches auth-token handling so independent Opus G2A mandatory and NOT skipped.
- what changed (3 files): channel-http adapter's upstream gateway token no longer defaults to the literal 'dev' when TORQCLAW_GATEWAY_TOKEN is unset. gatewayClient.ts adds pure exported resolveGatewayToken(env) returning TORQCLAW_GATEWAY_TOKEN || '' (matches the gateway's own || '' convention); server.ts calls it (production path == tested unit); tests/channel-http.test.ts +4 token tests. Two remaining 'dev' strings in channel-http src are comments documenting the removed behavior.
- security reasoning (G2A gate): '' is >= 'dev' in safety across all 4 config cases {gateway token set/unset} x {adapter set/unset} — never weaker, strictly safer when the gateway has a real token (old 'dev' could auth against a gateway whose token was literally 'dev'; '' never masquerades as a plausible secret). No server/engine code ever treated 'dev' as valid; both '' and 'dev' only ever worked via the tokenless-gateway-accepts-all dev-mode branch.
- completeness: channel adapter was the ONLY production upstream-token 'dev' default. Gateway's own GATEWAY_TOKEN was already || '' (out of scope). ops/*.mjs e2e scripts still hardcode client-side || 'dev' — harmless (rely on tokenless dev gateway), logged as a future test-hygiene candidate, out of 0F scope.
- tests/checks: typecheck 12/12; pnpm test 293/293 (+4); contracts:check 0; build 7/7; e2e-channel exit 0 (adapter sends '' to tokenless dev gateway, full HTTP 200 round-trip). NOTE: first local e2e-channel run failed 'ws not up' — environmental cold-start of the operator's real ~/.torqclaw/servers.json (filesystem+tradingview MCP), NOT a regression; passed on retry once servers warmed; G2A confirmed pass twice on a clean machine.
- G2A result (independent Opus 4.8): PASS — merge-ready. 4-case security matrix, completeness grep, teeth-check (revert to 'dev' broke 3 of 4 new tests). 1 info note (ops client-side 'dev', non-blocking).
- next ticket: TCLAW-0E (Python test suite bootstrap — full G1R scope, unlocks CI v2). Last Phase-0 ticket.
- blockers: operator merge approval.

### TCLAW-0E — Python test suite bootstrap + CI v2 pytest step

- date: 2026-07-09
- branch: ticket/tclaw-0e-python-tests @ 1eb8ff7 -> PR (base master). NOT merged — awaiting operator approval. First ticket to enable CI v2 (Python job).
- ticket: TCLAW-0E (PRD §9.0.2), last Phase-0 ticket. Full multi-model process (G1R design review completed before build).
- what changed (9 files): NEW engines/hermes_kernel/tests/{conftest,test_task_store,test_skill_queue,test_contracts,test_approval_hook,test_spend}.py — 74 hermetic pytest tests; pyproject.toml +[dependency-groups] dev=[pytest>=8] +[tool.pytest.ini_options] pythonpath/testpaths (NO pytest-asyncio); uv.lock regenerated (ADDS pytest 9.1.1 + iniconfig/packaging/pluggy/pygments, REMOVES nothing — runtime deps mcp/jsonschema untouched); ci.yml +pytest step after uv sync (CI v2). No mcp_wrapper/* source change.
- coverage: task_store (create/state/emit/metadata-roundtrip/cursor-ascending-not-hardcoded/since-filter/complete-telemetry/fail/no-op-unknown/persistence-across-reimport); skill_queue (queue/draft/approve-writes-SKILL.md/approve_edited/reject-no-dir/already-decided/unknown-id + documented-not-fixed path-escape test); contracts (real gateway-request.json fixture happy-path + 8 negatives + missing-schema RuntimeError); approval_hook (regex/predicate/block/first-block-only/grant/disabled/unknown + register() via injected stub); spend (stub 0.0/float/None + UNAVAILABLE-wins-precedence, fake-agent micros->USD + monkeypatched account-usage delta, pure helpers, hermes_available tuple-shape-only).
- G1R rulings enforced: OQ1 kernel-rooted imports + pythonpath=["."]; OQ2 hermes_available tuple-shape-only (never boolean — proven import-fragile); OQ3 no pytest-asyncio / no async server-stub; OQ4 skill_queue path-escape documented-not-fixed; OQ5 (G1R reversal, operator-accepted) 0E carries the ci.yml pytest step; GatewayRequest fixture only (no new frame fixtures — 10A/Epic 10).
- hermeticity (headline claim, PROVEN): 74 pass BOTH vendor-present AND vendor-absent (vendor renamed aside, still green) — vendor-independent; no network egress (agent=None stub + fake agents + monkeypatched account-usage); no ~/.torqclaw/~/.hermes pollution (env set to temp dirs at conftest module-top before first wrapper import; real home-dir mtime unchanged across run, verified).
- side-effect verified by G1D: uv sync uninstalled the editable hermes-agent==0.16.0 from .venv (never a declared dep; reached via sys.path.insert). Confirmed NOT a regression — engine starts in stub mode, e2e.mjs exit 0, uv.lock diff adds-only (removes no runtime package), documented FRONTIER setup (uv pip install -e vendor) unaffected.
- tests/checks: uv run pytest 74/74 (present) + 74/74 (vendor-absent); pnpm typecheck 12/12; pnpm test 293 (TS unchanged); contracts:check 0; build 7/7; engine stub smoke + e2e.mjs exit 0.
- G2A result (independent Opus 4.8): PASS. Ran vendor-absent path itself, before/after home-dir mtime check, network-egress grep, teeth-check (neutered validate_gateway_request broke exactly the 8 negatives, restored clean). 2 non-blocking notes: session tempdir not cleaned (cosmetic, isolation intact); pytest pinned 9.1.1 (>=8 floor).
- next ticket: Phase 0 COMPLETE after 0E merges. Next is the Visible Trust MVP phase — item 5 in the build order: Route explanation extension (Epic 2, extends RouterDiagnostics additively).
- blockers: operator merge approval. CI-green on the PR (first CI-v2 run with the Python job) is the milestone to confirm post-push.

### TCLAW-2A — additive RouterDiagnostics route-explanation extension (Phase 1, Epic 2)

- date: 2026-07-09
- branch: ticket/tclaw-2a-router-diagnostics @ 33b983c (commits 7016ecb feat, 33b983c test follow-up) -> PR (base master). NOT merged — awaiting operator approval. First Phase 1 (Visible Trust MVP) ticket.
- ticket: Epic 2 §9.2.4, scoped to RouterDiagnostics extension ONLY (UI/profiles/preview/receipt all deferred to later Epic 2 tickets).
- what changed (3 files): routing.ts extends RouterDiagnostics in place with 6 OPTIONAL fields (ruleId enum, humanReason, blockedAlternatives[], overridable, safetyLock, profile) + RouterRuleIdSchema + BlockedAlternativeSchema; engine.ts populates them per-rule via a RULE_META table, score/reason/tier byte-identical; router.test.ts +60 tests (new field-coverage block + 3 exact-score pins), 19 existing tests UNMODIFIED.
- G1R rulings enforced: LOCAL_INTENT overridable:false but NO safetyLock (only 3 locks: PRIVACY_OVERRIDE->SENSITIVE_DATA, USER_LOCAL_ONLY->USER_LOCAL_ONLY, LOCAL_TOOL_INTENT->LOCAL_TOOL_INTENT); profile added-but-unset; 19 existing tests unmodified; reason byte-identical; blockedAlternatives limited to the one non-chosen tier (honest). humanReason also fixes, data-side, the console's prior LOCAL_INTENT/LOCAL_TOOL_INTENT mislabel (no console file touched).
- no-behavior-change invariant (headline) PROVEN THREE WAYS: (1) G1D captured a pre-change score/reason/tier baseline for 10 cases, diffed after = IDENTICAL; (2) 19 existing router tests pass unmodified (129 insertions, 0 deletions); (3) G2A independently ran a 30-scenario parent-vs-current execution comparison incl. adversarial edges (conf 0.5 boundary, ctx 8192/8193, warm-latency rule-3 skip, PREFER_CLOUD+privacy) — all 30 match.
- cross-language: RouterDiagnostics travels only in GatewayEvent.metadata (z.unknown), NOT emitted/validated — additive fields = ZERO drift; contracts:check green, generated-schema dirs clean, Python 74 pass unaffected.
- tests/checks: typecheck 12/12; pnpm test 353 (router 79); contracts:check 0; build 7/7 with zero generated-schema diff; uv run pytest 74.
- G2A result (independent Opus 4.8): PASS. Byte-identity gate satisfied (30-scenario exec comparison); teeth-check confirmed tests guard tier+prefix. 1 informational finding (FRONTIER rule scores unpinned by tests) — G1D closed it immediately with the 33b983c exact-score follow-up rather than defer.
- next ticket: Epic 2 later sub-features (route preview, why-this-route panel, routing profiles, override stats) OR item 6 in the build order: Receipt projection + receipt page (Epic 4). Recommend confirming Phase 1 sequencing with operator.
- blockers: operator merge approval. CI-green on PR is the post-push gate.

### TCLAW-4A — run_receipts deterministic projection foundation (Phase 1, Epic 4)

- date: 2026-07-09
- branch: ticket/tclaw-4a-receipt-projection @ 498b9e1 -> PR (base master). NOT merged — awaiting operator approval.
- ticket: Epic 4 §9.4.3, backend/projection foundation ONLY (receipt page UI, export, redaction all deferred). First product-runtime change (dispatch.ts) since Phase-0 security work.
- what changed (5 files): schema.sql +run_receipts table (24 cols incl projection_version, evidence_start_seq/end_seq, created_at/updated_at) + idx_run_receipts_session + idx_events_request; NEW packages/gateway/src/receipts.ts (projectReceipt pure SELECT-only, materializeReceipt UPSERT keyed on task_id preserving id+created_at on conflict, safeMaterializeReceipt guarded wrapper, rebuildReceipt/Session/All); dispatch.ts +import +5 safeMaterializeReceipt hooks at the 5 terminal sites (success :175, blocked :192, failed :225, frontier-unavailable :148, tool-denied :110); NEW ops/receipts-rebuild.mjs CLI (--task/--session/--all[--stale]); NEW tests/receipt-projection.test.ts (18 tests). No storage.ts/events.ts/contracts/console/Python/ci change.
- design: run_receipts is a deterministic materialized projection — pure function of persisted events+tasks+tool_approvals (event log = source of truth). Rebuildable + idempotent (task_id UNIQUE upsert, id/created_at preserved on conflict). projection_version for logic-change migrations. Runs only after the terminal event is persisted, wrapped so a throw can never break completion.
- G1R rulings enforced: source_channel copy-verbatim from TOP-LEVEL request_json.sourceChannel (not payload) + non-authoritative; full_receipt_json supersedes buildReceipt (buildReceipt untouched); budget_source/cost_enforceable/safe_export_json NULL for 4A; evidence seq as 2 queryable columns; per-emit-site hook (not events.ts — projects too early); two-tier idempotency; tool-name parser strips "Executing " prefix covering BOTH local (ollama.ts) and cloud (hermes_runner.py) emitters.
- FAILURE-CASCADE GUARD (headline safety): G1R found an unwrapped projector throw on the success path would be caught by the outer try/catch and flip completed->failed + emit phantom ERROR. Mitigation: safeMaterializeReceipt (own try/catch, never rethrows) at all 5 sites; 0 bare materializeReceipt calls (G1D + G2A both grep-verified 5 safe / 0 bare). G2A ran its OWN teeth harness replicating dispatch control flow with the table dropped: BARE form corrupted completed->failed+phantom ERROR, WRAPPED form stayed completed no ERROR — proving the wrapper is the sole effective barrier.
- no-fabrication: verified in code AND against live run_receipts rows — failed/blocked tasks with no telemetry leave cost/iterations/elapsed NULL; route_diagnostics NULL when no TIER_SELECTED; budget_source/cost_enforceable/safe_export_json always NULL. result_state='blocked' derived from completed+telemetry.blockedOn.
- tests/checks: typecheck 12/12; pnpm test 371 (receipt 18); contracts:check 0 (run_receipts not emitted, generated dirs clean); build 7/7; uv run pytest 74; e2e.mjs exit 0 (completed) + e2e-approval.mjs exit 0 (blocked->approve->completed). Live DB spot-check confirmed real run_receipts rows: blocked task result_state='blocked'/blocked_on set, approved re-run result_state='completed'/tools_called_json=['filesystem__write_file'].
- G2A result (independent Opus 4.8): PASS — merge-ready. All 10 operator focus points verified with file:line + live evidence; failure-cascade gate proven with G2A's own bare-vs-wrapped teeth harness; projector proven SELECT-only (one write, run_receipts only); zero scope creep (5 files, 834 insertions, 0 deletions). No findings above the gate.
- next ticket: Epic 4 receipt PAGE / replay timeline UI (consumes run_receipts) OR safe-export redaction, OR item 7 in build order: Cost Control Center (Epic 1). Recommend confirming Phase 1 sequencing with operator.
- blockers: operator merge approval. CI-green on PR is the post-push gate.

### TCLAW-4B-1 — receipt read surface backend (Phase 1, Epic 4; first half of the 4B split)

- date: 2026-07-09
- branch: ticket/tclaw-4b1-receipt-read-surface @ c5b8a7d (commits fb649ee feat, c5b8a7d refactor) -> PR (base master). NOT merged — awaiting operator approval. G1R split 4B into 4B-1 (backend) + 4B-2 (console panel, later).
- ticket: Epic 4 §9.4.3/20.3, BACKEND read surface ONLY (console panel/replay UI is 4B-2).
- what changed (14 files): commands.ts +2 ClientCommand variants (LIST_RECEIPTS session-scoped by sid no param; GET_RECEIPT taskId+includeEvents); ClientCommand.json regenerated both dirs (only the 2 variants, EXPECTED_FILES still 4); receipts.ts listReceipts/getReceipt SELECT-only + handleListReceipts/handleGetReceipt exported handlers + MAX_REPLAY_BYTES(512KB); sessions.ts getEventsForRequest (SELECT, reuses rowToEvent); events.ts publishOnly (transient seq-less, no INSERT, single-session); server.ts switch delegates to the extracted handlers; authz.ts + schema.sql docstrings (run_receipts now read-over-wire); tests: fixtures + contracts-fixtures + authz + NEW receipts-read.test.ts (9 tests, real-handler-driven).
- G1R changes enforced (all 4): dead context-toggle omitted (contextChars only, no assembledContext in projection — display side is 4B-2 anyway); replay inertness handler-level (read handlers dispatch/mutate nothing — no APPROVE_TOOL/APPROVE_SKILL/tool_approvals reachable); ownership check BEFORE getEventsForRequest (foreign taskId not an oracle — identical receipt:null for absent+foreign); oversize all-or-marker (events=null + eventsOmitted{reason:'too_large',eventCount,seq range}, never partial). Plus docstring fixes.
- security invariants (G2A gate, all PASS): (a) NO write path reachable from reads — handlers call only SELECT helpers + publishOnly; (b) foreign taskId NOT an oracle — absent+foreign byte-identical; (c) transient responses NOT persisted — publishOnly seq-less, no INSERT, publishes to commanding session only. WS command over HTTP (Python copy-only, drift gate green, no new auth surface).
- tests/checks: typecheck 12/12; pnpm test 388 (receipts-read 9); contracts:check 0 (schema regen only the 2 variants); build 7/7; uv run pytest 74 (Python unaffected, only copied ClientCommand.json); e2e.mjs + e2e-approval.mjs exit 0 (new commands + publishOnly don't break dispatch).
- G2A result (independent Opus 4.8): PASS. Full audit of fb649ee (10 focus points, own teeth-check injecting insertEvent into publishOnly -> 3 tests fail -> revert). One low finding — no-write tests replicated the handler inline — G1D did NOT accept relying on manual audit for a trust surface: sent back to Builder, who extracted the handler bodies into testable exported fns (server.ts un-importable headlessly due to import-time side effects) and added real-handler integration tests + run_receipts snapshots; G2A re-confirmed the delta c5b8a7d (pure refactor, teeth-check injecting tool_approvals UPDATE -> tests 6+8 fail -> revert). Gap closed.
- next ticket: TCLAW-4B-2 — console receipt panel + read-only replay rendering (consumes LIST_RECEIPTS/GET_RECEIPT; pure display helpers unit-tested; readOnly EventRow with inert callbacks per G1R change #2).
- blockers: operator merge approval. CI-green on PR is the post-push gate.

### TCLAW-4B-2 — console receipt panel + read-only replay (Phase 1, Epic 4; completes the receipt/replay feature)

- date: 2026-07-09
- branch: ticket/tclaw-4b2-console-panel @ d484a93 -> PR (base master). NOT merged — awaiting operator approval. Second half of the 4B split; consumes the merged 4A projection + 4B-1 read surface.
- ticket: Epic 4 §9.4.3/§20.3, CONSOLE RENDERING ONLY. First substantial React ticket in the program.
- what changed (4 files, console+tests only): NEW apps/console/src/components/ReceiptsPanel.tsx (list from LIST_RECEIPTS, detail + replay from GET_RECEIPT, oversize marker, useState snapshots); friendly.ts +7 pure helpers; TorqTerminal.tsx additive (receipts toggle, mount panel overlay, EventRow early-return suppressing receiptView/receiptList frames, canRenderAction into live guards as defense); tests/friendly.test.ts +28. No backend/contract/schema/projection/gateway/Python change.
- SAFETY (the point of the ticket): replay is display-only STRUCTURALLY. Replayed events render through a standalone ReplayEventRow whose props are plain data ({row, approvalsByTool}) — a module-level sibling of ReceiptsPanel, so it cannot close over sendCommand; zero function-typed props; renders no approval card / recovery chip / tool card. A replayed PENDING_APPROVAL/ERROR shows inert text + decided status (plain lookup). No path to APPROVE_TOOL/APPROVE_SKILL/SUBMIT_PROMPT. This matters because ERROR->SUBMIT_PROMPT retry has NO server idempotency guard (would launch a real new run/spend).
- G1R changes enforced: two-layer defense (canRenderAction pure predicate + separate no-callback ReplayEventRow — G1R REQUIRED CHANGE 1: no live callback in lexical scope, not no-op props); RULE_LABELS set-equality drift guard vs RouterRuleIdSchema.options (CHANGE 2); single early-return suppressing panel frames from live log (CHANGE 3, bug-a); ring-buffer snapshot into useState (bug-b); 6 field-name pitfalls (taskPrompt sibling, selectedTier not tier, toolsCalled not tools, etc.); no assembledContext toggle (contextChars only). Operator helper set added: field, RULE_LABELS, formatReceiptState, formatCostField, formatRouteDiagnostics, toReplayEventRows, canRenderAction — all pure, all unit-tested.
- no-fabrication: field omits null/undefined/'' but renders real 0; formatCostField null->("not recorded", asserted not "$0.00"); budget_source/cost_enforceable always "not recorded"; failed runs no fabricated 0.
- no-DOM-test rationale (G1R+G2A ruled sound): safety is a compile-time/lexical property (separate component, no dispatch surface, no callback in scope) covered by typecheck + grep + the canRenderAction unit matrix — a DOM test would add nothing the structure doesn't already guarantee.
- tests/checks: typecheck 12/12; pnpm test 415 (friendly 48: +28); contracts:check 0 (no schema touched); build 7/7 (Next compiled the new component); uv run pytest 74.
- G2A result (independent Opus 4.8): PASS — merge-ready. Proved the safety boundary 3 ways (lexical-scope, type-level, whole-tree grep); own teeth-check on canRenderAction (mutate->fail->restore); confirmed replay safety independent of canRenderAction; snapshot survives eviction; zero scope creep (git diff master -- packages/ engines/ empty). 1 cosmetic finding ("too large to load" wording), non-blocking.
- secret-scan note: friendly.ts privacy-DETECTION regex + friendly.test.ts fake key fixtures matched the scanner but are PRE-EXISTING (unchanged from master) and defensive, not leaks — 4B-2 added no new secret-shaped content.
- next ticket: the Visible Trust receipt/replay surface is COMPLETE (2A route data + 4A projection + 4B-1 read + 4B-2 UI). Next build-order item 7: Cost Control Center (Epic 1) — session/daily caps + spend dashboard; OR remaining Epic 2 sub-features (route preview panel using 2A fields, routing profiles). Recommend confirming Phase 1 sequencing with operator.
- blockers: operator merge approval. CI-green on PR is the post-push gate.

### TCLAW-1A-core — session/daily spend cap enforcement foundation (Phase 1, Epic 1 Cost Control Center)

- date: 2026-07-10
- branch: ticket/tclaw-1a-core-spend-enforcement @ cfca872 -> PR (base master). NOT merged — awaiting operator approval. First half of the G1R-mandated 1A split (1A-attr = Python costSource tag, fast-follow).
- ticket: Epic 1 §9.1, BACKEND/GOVERNANCE only (dashboard UI = TCLAW-1B; deferred). This is the "budget before spend" pillar + where 4A's NULL budget_source/cost_enforceable get real data.
- what changed (8 files): schema.sql +spend_ledger (self-migrating, gateway-DB-only); NEW spend.ts (env-only cap resolution, sessionTotal/dailyTotal UTC SUMs, evaluateCaps, resolveBudgetWithSource, recordSpendSafe guarded); dispatch.ts (cap GATE at :156-190 FRONTIER-only after taskStore.create before any provider call; terminal recordSpendSafe; budgetSource threaded onto telemetry); events.ts (taskStore.fail +optional telemetry param, backward-compatible); hermes.ts (CircuitBreakerError +lastCostUsd); receipts.ts (budget_source/cost_enforceable from real telemetry, replacing 4A nulls); tests/spend-caps.test.ts (34) + receipt-projection.test.ts (updated). No contracts/Python/console/UI.
- G1R corrections enforced (both): (A) breached-task cost gap — taskStore.fail now persists telemetry_json AND CircuitBreakerError carries last-known costUsd, so a budget-breached FRONTIER task records its spend in ledger + receipt (retry loop of always-breaching tasks accumulates in the running SUM, no longer escapes the cap). (B) NO client cap-raise — caps are env-only (TORQCLAW_SESSION_CAP_USD/TORQCLAW_DAILY_CAP_USD); no BudgetPolicy on any ClientCommandSchema; a test proves zod strips a smuggled sessionCapUsd. Client per-task maxCost only lowers per-task exposure.
- hard invariants (G2A gate, all PASS): (a) enforcement BEFORE spend — gate returns before executeHermesTask, $0 partial spend for a refused task; (b) no client cap-raise; (c) breached tasks don't escape accounting (proven end-to-end); (d) $0/<=0/unset cap = unlimited (inherits resolveBudget >0-rule, doesn't brick cloud). Plus: retries re-hit the gate (new task_id), replay is read-only (no spend.js import in receipts), UTC daily window, provider-unavailable = NULL cost + attribution 'unavailable' excluded from SUM never $0, LOCAL_EDGE never blocked/charged (caps orthogonal to routing). CAP_EXCEEDED refusal names the cap + exact env var + UTC reset + free RETRY_LOCAL + conservative-totals caveat.
- G1R rulings: UTC daily; budget_source stays per_task/env_default/unlimited; gate at dispatch chokepoint; >= post-hoc (no pricing table); ERROR + metadata.kind='CAP_EXCEEDED'; 2-way attribution (reported/unavailable) now, 3-way (exact/account_delta) in 1A-attr with flexible-TEXT attribution column so no schema change later; budget_source via telemetry_json. Env-only caps (BudgetPolicy dropped per correction B).
- tests/checks: typecheck 12/12; pnpm test 453 (spend-caps 34); contracts:check 0; build 7/7; uv pytest 74 (no Python); e2e-budget exit 0 (mid-run breaker still fires identically — CircuitBreakerError change backward-compatible), e2e exit 0 (no cap env -> gate no-ops).
- G2A result (independent Opus 4.8): PASS — merge-ready. Verified all 12 focus points file:line; teeth-check (disable evaluateCaps -> 6 tests fail incl gate-ordering -> restore green). 2 info notes (always-unreporting-provider spend can't accumulate toward cap = the documented never-fabricate boundary, improved by 1A-attr; cost-less FRONTIER successes write NULL-cost audit rows) — neither a defect.
- next ticket: TCLAW-1A-attr — hermes_runner.py costSource attribution tag (credits->exact / account_delta / unavailable), upgrading the ledger attribution to 3-way in place (no schema change), + bridge passthrough + projector map + pytest. Then TCLAW-1B (Cost Control Center dashboard UI).
- blockers: operator merge approval. CI-green on PR is the post-push gate.

### TCLAW-1A-attr — 3-way spend attribution (exact/account_delta/unavailable) (Phase 1, Epic 1; fast-follow to 1A-core)

- date: 2026-07-10
- branch: ticket/tclaw-1a-attr-costsource @ dfa4b8b (commits 202955a feat, dfa4b8b test) -> PR (base master). NOT merged — awaiting operator approval. Last Epic-1 BACKEND ticket (TCLAW-1B dashboard UI is next). First Python source change since TCLAW-0E.
- ticket: upgrades 1A-core's 2-way ledger attribution (reported/unavailable) to 3-way (exact/account_delta/unavailable) across Python + TS. Attribution-LABELING only — no enforcement change.
- what changed (8 files + 1 test-delta): hermes_runner.py get_spend_usd -> tuple(cost, costSource), 5 return sites tagged (credits->exact, account-usage delta->account_delta, None->unavailable); server.py all 3 callers unpack (unavailable warning preserved); bridge/hermes.ts CircuitBreakerError +lastCostSource (mirrors lastCostUsd); dispatch.ts threads costSource on SUCCESS + BREACH recordSpend; spend.ts recordSpend 3-way from the tag (unavailable->NULL); receipts.ts surfaces costSource in full_receipt_json (no column); test_spend.py 8 tests + regression; spend-caps.test.ts 34->44. NO schema/contracts/task_store/console.
- G1R changes enforced: OQ3-b breach-path fix (CircuitBreakerError.lastCostSource so a breached task keeps cost + label, NOT NULL'd -> would regress 1A-core correction A); tuple return (lockstep caller unpacks — one commit); 'exact' not 'credits'; full_receipt_json not a column; one ticket not split.
- hard invariants (G2A gate, all PASS): (a) breach cost NOT NULL'd (correction A preserved); (b) all Python callers unpack (unavailable warning still fires); (c) enforcement math byte-identical (no SUM/evaluateCaps change, no WHERE attribution filter); (d) additive telemetry boundary, no drift. Plus: account_delta counts in SUM labeled conservative (never netted/fixed), unavailable NULL excluded never $0, exact/account_delta structurally unconfusable (credits early-return), legacy 'reported' rows still sum (value-agnostic).
- G2A result (independent Opus 4.8): PASS-WITH-NOTES on first pass — all 10 focus points verified; but the MANDATORY teeth-check found the 'load-bearing' breach test called recordSpend directly (hardcoded costSource), never drove dispatch() — sabotaging dispatch's breach threading left all 461 green (no teeth for the real regression). G1D did NOT accept a toothless guard for the operator-flagged load-bearing regression: sent back to Builder, who added two dispatch-level tests driving a real breach through dispatch() (mock bridge -> genuine CircuitBreakerError with lastCostUsd+lastCostSource -> poll actual spend_ledger row) asserting non-NULL cost + label. G2A re-confirmed delta dfa4b8b: test-only, teeth real (sabotage fails exactly the 2 new tests, unit tests stay green, restore clean), 463 tests green twice no flake.
- tests/checks: typecheck 12/12; pnpm test 463 (spend-caps 44); contracts:check 0; build 7/7; uv pytest 75 (test_spend.py tuple unpacks); e2e-budget exit 0 (breaker still fires -> CircuitBreakerError 3-arg signature safe), e2e exit 0.
- next ticket: TCLAW-1B — Cost Control Center dashboard UI (consumes spend_ledger session/daily totals + the 3-way attribution + budget_source/cost_enforceable receipt fields). Console rendering, read-only, like 4B-2. Then remaining Epic 2 sub-features or other PRD priority items.
- blockers: operator merge approval. CI-green on PR is the post-push gate (Python job exercises the updated test_spend.py).

### TCLAW-1B — Cost Control Center dashboard UI (Phase 1, Epic 1; completes the spend-cap feature)

- date: 2026-07-10
- branch: ticket/tclaw-1b-cost-dashboard @ a408212 -> PR #11 (base master). NOT merged — awaiting operator approval. Completes the Epic 1 spend-cap surface; consumes merged 1A-core + 1A-attr.
- ticket: Epic 1 §9.1, CONSOLE RENDERING ONLY. Final user-facing surface for session/daily spend caps. G1R (Opus 4.7) + G2A (Opus 4.8) mandatory reviews completed.
- what changed (4 files + 2 new): NEW apps/console/src/components/CostPanel.tsx (read-only dashboard, live spend snapshot via new GET_COST_SUMMARY command, ring-buffer safe via useState, no state-mutation affordances); NEW tests/spend-cost-summary.test.ts (9 tests covering command path, attribution counts, cloud-task count, provider summary filtering); NEW tests/fixtures/client-command.get-cost-summary.json (generated fixture). TorqTerminal.tsx (cost-summary command integration + meta.costSummary suppression guard). friendly.ts +7 pure helpers (formatCap, formatRemaining, formatAttribution, formatLedgerCost, formatCapState, formatDailyTotalLabel, formatProviderSummaryRow) + formatCostField now maps real budget_source/cost_enforceable from spend_ledger; schema.sql docstring fix; spend.ts stale comments fixed. No contracts/backend logic/Python/receipts change.
- NEW ClientCommand GET_COST_SUMMARY: SELECT-only, zero-write handler (publishOnly, transient seq-less response), no sessionId param — reads recent ledger + session-scoped attribution counts + session-scoped cloud-task count + per-provider summary (excluding NULL-cost rows from $). Authz: explicit channel/node deny via updated authz.ts (GET_COST_SUMMARY operator-only).
- G1R rulings enforced (4 required changes, all folded): RETRY_LOCAL removed (dispatches SUBMIT_PROMPT, not a dashboard affordance); explicit operator-only authz deny for GET_COST_SUMMARY; session-scoped attribution-count query (not summed globally); stale comments fixed in friendly.ts formatCostField + schema.sql. Design decisions: single GET_COST_SUMMARY command (not split); deferred retry-recs/breaker-firings/top-expensive-runs; included cloud-task-count + provider-summary with "N unrecorded" caveat for unavailable; account_delta badge "estimated · account-level · conservative" + tooltip; dailyTotal cross-session but operator-only + labeled "all sessions (UTC day)".
- hard invariants (G2A gate, all PASS): (a) dashboard read-only (zero writes, no cap edit/raise/policy command); (b) no new backend cap command (GET_COST_SUMMARY SELECT-only); (c) no run/retry/SUBMIT_PROMPT affordance (panel safe — no dispatch surface in render); (d) no client-side spend re-summing (displays ledger totals, no arithmetic). Plus: unavailable cost never $0 (NULL excluded), account_delta always labeled estimated/conservative, daily total labeled UTC + "all sessions", receipt refs read-only (consumedFrom run_receipts links to task_id, inert).
- G2A result (Opus 4.8): PASS — zero findings. Enforcement math byte-identical (pure-additive spend.ts diff: new SELECT helpers, no deletion lines). All 10 operator focus points verified with file:line evidence. Load-bearing tests sabotage-verified with teeth (zero-write count: events table unread, NULL cost marked not $0, account_delta label respected, seq-less frame, operator-only authz enforced).
- tests/checks: typecheck 12/12; pnpm test 505 (friendly 48, spend-cost-summary 9); contracts:check 0; build 7/7; uv pytest 75; CI run 29104886463 GREEN (all stages + 6 e2e scripts).
- files changed: 16 (+960/-10). New: CostPanel.tsx, spend-cost-summary.test.ts, client-command.get-cost-summary.json.
- next ticket: remaining Epic 2 sub-features (route preview panel, why-this-route panel, routing profiles, override stats) OR other PRD priority items. Phase 1 spend-cap feature (1A-core + 1A-attr + 1B) COMPLETE pending merge.
- blockers: operator merge approval. CI-green on PR is the post-push gate.

### TCLAW-2B — "Why this route?" receipt route-transparency panel (Phase 1, Epic 2; consumes 2A RouterDiagnostics)

- date: 2026-07-10
- branch: ticket/tclaw-2b-route-panel @ 407bef3 -> PR #12 (base master). NOT merged — awaiting operator approval. Pure UI consumer of merged TCLAW-2A RouterDiagnostics extension.
- ticket: Epic 2 §9.2.4, second sub-feature (route explanation extension to receipts). Enriches read-only receipt Detail "Route diagnostics" section. No backend/contract/router/schema change; no new command; no override action; no route behavior change.
- what changed (3 files, +302/-2): ReceiptsPanel.tsx enriched ReceiptDetail routeRows composition with honest three-state lock taxonomy + all blockedAlternatives rendering (no ≤1 cap) + omitted profile field; NEW friendly.ts +4 pure helpers (formatLockState, formatBlockedAlternatives, formatProfile, formatRouteExplanation) + formatRouteDiagnostics regression guard exported; tests/friendly.test.ts +26 (three-state branches, TWO anti-conflation tests, length-2 blockedAlternatives, profile omission, partial-diag honesty, lock-taxonomy drift guard vs RULE_SHAPE mirror).
- lock taxonomy (core deliverable): honest THREE-state from engine.ts RULE_META — (a) hard safety lock [safetyLock present: PRIVACY_OVERRIDE/USER_LOCAL_ONLY/LOCAL_TOOL_INTENT] "Locked — safety rule: X"; (b) firm no-lock [LOCAL_INTENT overridable:false, no safetyLock] "Fixed for this task" (affirmative, no "not a safety lock"); (c) overridable [4 heuristic rules] "Router preference — can be overridden". Branch (a) checked BEFORE (b) so a hard privacy lock is never demoted.
- blockedAlternatives: renders ALL elements (no ≤1 cap), exact wire-tested "would have used {tier}, but: {why}".
- profile: omitted via field() (2A never populates it), never "default"/"not set"/"none".
- missing/partial diagnostics honest: null → "no routing record"; {score,reason,tier}-only → raw reason, no fabricated ruleId.
- deferred: live current-task chip (TorqTerminal.tsx untouched) — optional/cut-line, requires requestId-anchored snapshot wiring; core deliverable green without it.
- G1R (Opus 4.7) ruling: APPROVE-WITH-CHANGES — 4 required folded (three-state taxonomy; LOCAL_INTENT affirmative wording; render all blockedAlternatives no cap; profile omit not default/not-set). Enrichment-primary + optional live chip deferred, overlay deferred.
- G2A (Opus 4.8) result: PASS-WITH-NOTES. Zero blocker/major findings, 3 nits (nit: formatRouteExplanation labels tier row 'route' + slightly misleading tierLabel comment; read-only boundary confirmed clean; regression guard intact). Teeth verified by live code mutation — branch-order swap, string-collapse, and hard-lock-set removal each turn named tests red. Limitation: drift guard mirrors engine.ts RULE_META locally (RULE_META not exported, scope forbids adding export) so it won't auto-catch a silent engine-side safetyLock edit, but RouterRuleIdSchema key-set assertion catches enum drift and limitation documented in test comment — acceptable tradeoff.
- tests/checks: typecheck 12/12; pnpm test 531; contracts:check exit 0; build 7/7; uv pytest 75.
- CI run 29108298503 GREEN (all stages + 6 e2e scripts).
- files changed: 3 (+302/-2). Modified: ReceiptsPanel.tsx, friendly.ts, tests/friendly.test.ts.
- next ticket: remaining Epic 2 sub-features (route preview panel, routing profiles, override stats) OR other PRD priority items. Phase 1 route-transparency surface (2A router-data + 2B panel) COMPLETE pending merge.
- blockers: operator merge approval. CI-green on PR is the post-push gate.

### TCLAW-2C — Live current-task route chip (Phase 1, Epic 2; deferred cut-line follow-up to 2B)

- date: 2026-07-10
- branch: ticket/tclaw-2c-route-chip @ ce5b178 -> PR #13 (base master). NOT merged — awaiting operator approval. Deferred cut-line follow-up to merged TCLAW-2B.
- ticket: Epic 2 §9.2.4, third sub-feature (live in-flight route decision chip). Pure UI consumer. No backend/router/contract/schema change; no new command; no dispatch surface; no overlay.
- what changed (3 files, +152/-2, all MODIFIED, no new files): apps/console/src/components/friendly.ts adds the pure selectActiveRouteDiag(events, activeRequestId) selector (returns the current task's TIER_SELECTED diag from live events); apps/console/src/components/TorqTerminal.tsx adds requestId-keyed useState snapshot + write-on-present useEffect (mirrors ReceiptsPanel keyed useMemo + write-not-clear pattern) + anchor-gated render (chip visible only if activeRequestId NOT busy) + chip JSX (tier label + lock-state summary, "why" in title tooltip); tests/friendly.test.ts +8 selector tests (sabotage-verified: ignore-anchor, return-on-null-anchor, drop-metadata-guard each turns named tests red).
- core invariants (load-bearing mechanisms): (1) render source is EXACTLY `activeRequestId ? routeSnapshot[activeRequestId] : null` — never a remembered id, never busy-checked inside render, never the selector-over-events directly, so a stale route can never resurface for a different task. (2) write-on-present snapshot (mirror ReceiptsPanel pattern) — never clears on absent, so a long task that evicts its own TIER_SELECTED frame from the 1000-event ring keeps its chip; the pure selector feeds the write, the useState snapshot is the render source.
- gated on activeRequestId NOT busy: PENDING_APPROVAL excluded from busy, so a task paused for approval is still the current task — chip stays visible during the pause and swaps to the re-minted task's route (new requestId) on APPROVE. Documented as intentional.
- chip content: tier + lock-state inline; one-line "why" in title tooltip. No blockedAlternatives (receipt-panel territory). No new formatRouteChip helper — composes existing tested 2B helpers (tierLabel + formatLockState) inline so the anti-conflation branching is not forked.
- G1R (Opus 4.7): APPROVE-WITH-CHANGES — 3 required folded (pin the live-anchor render invariant; split pure selector from render source with write-on-present; terminal behavior via anchor gate, snapshot not cleared). Rulings: gate on activeRequestId not busy; tier+lock inline with why in tooltip; no new formatRouteChip; requestId-keyed snapshot + extract selectActiveRouteDiag.
- G2A (Opus 4.8): PASS-WITH-NOTES — all 10 focus points PASS, zero blocker/major/CHANGES-REQUIRED findings. Teeth verified empirically: 3 selector sabotages (ignore-anchor, return-on-null-anchor, drop-metadata-guard) each turn named tests red. One informational note / disclosed gap: the two COMPONENT-level invariants (RC-1 render source + write-on-present effect) are inspection-guarded, NOT unit-tested (no React render harness in the test file) — a Builder wiring the selector directly into render or adding clear-on-absent would keep all 8 selector tests green. G2A ruled this acceptable given a display-only chip with zero dispatch surface and a purely cosmetic failure mode; noted for the record.
- tests/checks: typecheck 12/12; pnpm test 539 (friendly 8 new); contracts:check 0; build 7/7; uv pytest 75.
- CI run 29111292862 GREEN (all stages + 6 e2e scripts).
- files changed: 3 (+152/-2), all modified (no new files): apps/console/src/components/TorqTerminal.tsx, apps/console/src/components/friendly.ts (pure selector added), tests/friendly.test.ts.
- next ticket: remaining Epic 2 sub-features (route preview panel, routing profiles, override stats) OR other PRD priority items. Phase 1 route-transparency surface (2A + 2B + 2C) COMPLETE pending merge.
- blockers: operator merge approval. CI-green on PR is the post-push gate.

### TCLAW-QA-1 — Console React component test harness (Phase 1, QA/TEST-INFRA; closes G2A inspection gaps on 2B/2C/receipts/cost)

- date: 2026-07-10
- branch: ticket/tclaw-qa-1-harness -> PR #14 (base master). NOT merged — awaiting operator approval. QA ticket (test-infra, zero product-component edits).
- ticket: QA / TEST-INFRA (closes component-level inspection gaps G2A flagged on 2B/2C/receipts/cost).
- what changed (3 files MODIFIED, 3 files NEW, zero product/tsconfig edits): MODIFIED package.json (3 devDeps: jsdom ^25, @testing-library/react ^16, @testing-library/jest-dom ^6), pnpm-lock.yaml (regenerated, --frozen-lockfile satisfied), vitest.config.ts (include glob widened to tests/**/*.test.{ts,tsx}, esbuild.jsx:'automatic' config-seam for console jsx:'preserve'→auto-transform, resolve.alias for react/react-dom→apps/console/node_modules pnpm strict-isolation); NEW tests/cost-panel.test.tsx (8 cases), tests/receipts-panel.test.tsx (6 cases), tests/torq-terminal-route-chip.test.tsx (5 cases). Total +19 new component test cases. MODIFIED vitest.config.ts adds environment seam: per-file `// @vitest-environment jsdom` docblock opts into jsdom, 539 existing node tests untouched.
- infrastructure design: jsdom for React components (DOM testing), node environment global preserves all 539 existing TS tests (no migration). Two config-only seams (no product runtime deps, no new tsconfig edits): (A) resolve.alias react/react-dom→apps/console/node_modules matches pnpm strict isolation (same mechanism as contracts alias); (B) esbuild.jsx:'automatic' enables auto-transform (console uses jsx:'preserve' for Next.js, esbuild won't auto-transform). G2A ruled both legitimate test-infra, not product-seams.
- test coverage (19 cases across 3 suites): cost-panel (8) — dispatches only GET_COST_SUMMARY action-subset, getAllByText verifies unlimited-cap duplicate nodes, account_delta labeled estimated/account-level/conservative, provider unrecorded caveat honest, unavailable never $0.00, breach-hint no button, no-dispatch teeth (positive presence + click-all + dispatch subset verified); receipts-panel (6) — replay read-only teeth (inert feeds dangerous PENDING_APPROVAL approvalId/queueId + ERROR recovery shapes, asserts zero buttons in replay subtree structurally after G2A fix, historical text inert), open-detail snapshot survives eviction, oversize marker, NULL honesty; torq-terminal-route-chip (5) — mounts REAL TorqTerminal via vi.mock('./useGatewayStream') zero source touch, proves invariants: A (render source = active-anchor snapshot), C (interleaved A/B shows B not A), D (terminal/null hides chip), no chip dispatch.
- invariant B disclosure (write-on-present survives eviction): "A active but its TIER_SELECTED evicted" state is NOT constructible from events alone (activeRequestId derives from same frame). Test proves constructible consequence (snapshot render-stability as log grows). Pure eviction-null covered by selectActiveRouteDiag test 5 in tests/friendly.test.ts. G2A ruled disclosure accurate + partial coverage acceptable.
- G1R (Opus 4.7) ruling: APPROVE-WITH-CHANGES — 4 required folded: (1) route-chip tests via vi.mock not deferred; (2) getAllByText for duplicate unlimited-cap nodes; (3) no-dispatch teeth with positive presence + allowlist-subset; (4) explicit afterEach(cleanup) since no globals:true.
- G2A (Opus 4.8) result: PASS-WITH-NOTES. Empirically sabotage-tested every safety-critical assertion — 3 of 4 teeth confirmed by product-sabotage (CostPanel no-dispatch, chip invariants C+D each FAIL when product sabotaged). Found ONE MEDIUM gap in receipts replay: subset-click-loop passed VACUOUSLY against regex-evading button (unmount-on-navigation defeats loop). FIXED in commit 1fc277e: asserts ZERO buttons in replay subtree structurally (label-independent, un-bypassable, matches ReplayEventRow no-callback invariant). Re-verified by sabotage (injecting regex-evading "proceed" button FAILS assertion; product sabotage reverted byte-identical). G2A confirmed config seams legitimate, invariant-B disclosure accurate, frozen-lockfile passes, zero product change, no existing test weakened.
- tests/checks: typecheck 12/12; vitest 558 (539 node + 19 new jsdom); contracts:check 0; build 7/7; uv pytest 75; pnpm install --frozen-lockfile passes.
- CI run 29115183659 (post-fix) GREEN (all stages + 6 e2e scripts).
- commits: 753d8ad (harness infra + 19 test cases) + 1fc277e (G2A finding #1 receipts-replay teeth fix).
- files changed: 3 modified (package.json, pnpm-lock.yaml, vitest.config.ts), 3 new (tests/cost-panel.test.tsx, tests/receipts-panel.test.tsx, tests/torq-terminal-route-chip.test.tsx). Zero apps/console/src or packages/* edits.
- next ticket: remaining Phase 1 deliverables or other PRD priority items. QA component-test harness COMPLETE pending merge.
- blockers: operator merge approval. CI-green on PR is the post-push gate.

### TCLAW-QA-2 — Live approval and recovery affordance component coverage (Phase 1, QA/TEST-ONLY)

- date: 2026-07-10
- branch: ticket/tclaw-qa-2-live-affordances -> PR #15 (base master). NOT merged — awaiting operator approval. QA ticket (test-only, zero product edits).
- ticket: QA / TEST-ONLY (proves LIVE write affordances in TorqTerminal dispatch the exact correct ClientCommand). Inverse of QA-1's replay tests.
- what changed (1 file NEW, zero product/config/dep/lockfile edits): NEW tests/torq-terminal-live-affordances.test.tsx (+346 lines). Test harness verbatim reuse from QA-1: vi.mock('./useGatewayStream') + real TorqTerminal mount + jsdom + docblock + scrollTo polyfill + afterEach cleanup + stream reset incl. sendCommand re-assign guarding mockReturnValueOnce leakage.
- coverage (14 cases across 5 describes): tool approval Allow/Deny → exact APPROVE_TOOL {approvalId,decision} no toolName + render-driven dedup; skill approval plain Allow / Approve-with-edits [textarea asserted before typing] / Deny-after-edit [REJECT with NO editedMarkdown — the shape-leak guard]; skill draft fetch → GET_SKILL_DRAFT + no-textarea-yet; ERROR recovery retry-at-$N [maxCostUsd present] / retry-no-budget [G1R RC-1: actual call arg + not.toHaveProperty('maxCostUsd') + DEFAULT_CONTROLS coupling documented] / run-on-this-machine [LOCAL_ONLY] / run-on-cloud [CLOUD_OK] / copy-diagnostic [clipboard.writeText called, sendCommand calledTimes(0)]; cancel happy [CANCEL_TASK {taskId:activeRequestId} + stopping/disabled] / send-dropped [mockReturnValueOnce(false)] / busy-gating negative [no stop during PENDING_APPROVAL pause; null-anchor branch documented non-constructible].
- anti-vacuous (G1R RC-2): every dispatch assertion gated behind getByText of the exact button label. Dedup honesty: render-driven decided-map unmount, NOT internal idempotency — documented in-test; scoped to approvals only (ERROR retry repeatable by design; cancel uses disabled-after-send). Live-vs-replay boundary cross-referenced to receipts-panel.test.tsx test 2 (not duplicated).
- G1R (Opus 4.7): APPROVE-WITH-CHANGES — 2 required folded (not.toHaveProperty for maxCostUsd absence; exact-button-label gating) + 4 suggested folded (Deny-after-edit case; textarea-rendered assertion; copy-diagnostic sendCommand-not-called; mockReturnValueOnce for send-dropped). Rulings: one combined file; objectContaining for SUBMIT_PROMPT; dedup approvals-only; cross-reference not re-assert.
- G2A (Opus 4.8): PASS-WITH-NOTES — ran a TEN-sabotage campaign against the product (each reverted byte-identical, verified by blob hash): every meaningful sabotage (stray toolName, editedMarkdown-on-REJECT end-to-end, injected budget, forced CLOUD_OK, wrong taskId, dropped setDecided, wrong draft action, mutated edit text, copyDiagnostic dispatching) turned exactly the intended assertion red. All 12 operator focus points PASS. Two INFORMATIONAL findings (no changes required): (1) the Deny-after-edit comment over-credits decideSkill :226 — the primary guard is the Deny button passing no text (:741), with :226 as defense-in-depth; (2) the run-on-this-machine assertion is actually pinned by the budget-'free' branch of buildSubmit (:45) rather than the mode override on a fresh mount — observable LOCAL_ONLY output still correctly asserted. Both are honesty-of-attribution comment nits, not test-behavior defects.
- tests/checks: typecheck 12/12; vitest 572 (558 + 14 new); contracts:check 0; build 7/7; uv pytest 75; pnpm install --frozen-lockfile passes.
- CI run 29121851468 GREEN @ 21a7f6c (all stages + 6 e2e scripts).
- commits: 21a7f6c (live-affordances 14 cases).
- files changed: 1 new (tests/torq-terminal-live-affordances.test.tsx). Zero product, zero config, zero deps, zero lockfile.
- next ticket: remaining Phase 1 deliverables or other PRD priority items. QA live-affordance coverage COMPLETE pending merge.
- blockers: operator merge approval. CI-green on PR is the post-push gate.

### TCLAW-2D-1 — Route preview backend read surface (PREVIEW_ROUTE) (Phase 1, Epic 2; first half of the 2D split)

- date: 2026-07-10
- branch: ticket/tclaw-2d-1-preview-backend @ 6890e7e -> PR #16 (base master). NOT merged — awaiting operator approval. G1R split 2D into 2D-1 (backend) + 2D-2 (composer UI, later). First contract change since 1B.
- ticket: Epic 2 §9.2.4, BACKEND read surface ONLY (composer UI/affordances is 2D-2). Backend half of the G1R-mandated 2D split (APPROVE-WITH-CHANGES + SPLIT-REQUIRED).
- what changed (10 files changed, +564/-0, purely additive): commands.ts +PREVIEW_ROUTE variant (operator-only ClientCommand with previewOf string nonce param 1-128 chars); ClientCommand.json regenerated both dirs (only the 1 variant, EXPECTED_FILES still 4); NEW packages/gateway/src/preview.ts handler: deliberately SKIPS emit('USER_PROMPT') (that persisted event feeds Tier-1 memory recall — emitting would poison memory with un-submitted drafts; the write stays dispatch-path-only), skips ROUTING/TIER_SELECTED (chip + receipt-projector pollution), responds via publishOnly only (seq-less, non-persisted, session-only frame). Frame carries verbatim RouterDiagnostics (2B helpers reuse as-is) + enrichment SCALARS only — NEVER assembledContext (Tier-2 FTS recall is global across sessions). Single-flight latch per session (concurrent second preview -> immediate honest dropped:'in_flight' frame; bounds classifier GPU occupancy; try/finally release proven even on throwing enrichment). server.ts switch case + publishOnly call; authz.ts explicit channel deny with rationale (unmetered local-inference trigger, no task/receipt/audit trail — not channel-grantable until rate limiting exists); node denied; operator allowed. NEW tests/preview-route.test.ts (9 tests). Modified: tests/authz.test.ts, tests/contracts-fixtures.test.ts. Byte-identical to master: enrich.ts, events.ts, sessions.ts, classifier.ts, dispatch.ts, router/engine.ts — live route behavior unchanged. Zero apps/console changes.
- G1R rulings enforced (all 4 APPROVE-WITH-CHANGES mandates + 1 SPLIT-REQUIRED): (1) previewOf nonce 1-128 chars echoed VERBATIM on every frame incl. the dropped frame (the 2D-2 staleness key — immune to edit-back races vs prompt-text equality, G1R RC-4); (2) explicit channel authz deny with rationale (RC-3); (3) single-flight latch in 2D-1, useMemory honored exactly (not re-submitted); (4) enrichment meta (classifierUsed/confidence) in frame for fidelity honesty; (5) SPLIT-REQUIRED: RC-1 (busy-memo guard) + RC-2 (verbatim caveat "Preview only. Enrichment and route may change when you submit.") are 2D-2 obligations, recorded for that ticket.
- hard invariants (G2A gate, all PASS): (a) zero write paths — no emit('USER_PROMPT'), ROUTING, TIER_SELECTED; zero-write proof = counts + state snapshots across events/tasks/tool_approvals/spend_ledger/run_receipts + full events dump + targeted USER_PROMPT/TIER_SELECTED/ROUTING count (all zero); (b) previewOf nonce echoed VERBATIM on every frame incl. the dropped frame; (c) no assembledContext/payload on the frame (dedicated leak test); (d) single-flight latch per session (concurrent second preview -> immediate dropped:'in_flight' frame; ONE inference proven via call counter; latch release proven via a third call; releases even on throwing enrichment via try/finally); (e) authz operator allow / channel explicit deny with rationale / node deny, driven through the real authorize(); (f) useMemory honored exactly. Plus: both classifier paths proven (unreachable-OLLAMA fallback honestly labeled KEYWORD_FALLBACK/DEFAULT; vi.spyOn LOCAL_LLM path) and the real rule hierarchy proven (sensitive:true -> PRIVACY_OVERRIDE/SENSITIVE_DATA/OLLAMA_LOCAL).
- tests/checks: typecheck 12/12; vitest 589 total (all pre-existing tests green unmodified; +8 net new per the Builder's stash-verified baseline); contracts:check 0 (both regenerated ClientCommand.json in sync); build 7/7; uv pytest 75. All invariants proven via named assertions in tests/preview-route.test.ts (9 tests) + authz + contract fixtures.
- CI run 29127125998 = success @ 6890e7e (all stages + 6 e2e, full gate green).
- G1R (Opus 4.7): APPROVE-WITH-CHANGES + SPLIT-REQUIRED — backend rulings folded: previewOf nonce staleness key (RC-4); explicit channel authz deny (RC-3); single-flight latch in 2D-1; useMemory honored exactly; enrichment meta (classifierUsed/confidence) in frame for fidelity honesty; near-threshold flags deferred. RC-1 (busy-memo guard) and RC-2 (verbatim caveat) are 2D-2 obligations, recorded for that ticket.
- G2A (Opus 4.8): PASS-WITH-NOTES — zero findings. THREE empirical sabotages, each caught by named assertions and reverted byte-identical (md5-verified): (a) injected real write (makeEmitter USER_PROMPT + taskStore.create) -> 8/9 tests failed; (b) prompt echoed as nonce (instead of input nonce) -> nonce tests failed; (c) latch check removed -> single-flight test failed (second inference ran). Latch proven to release on throwing enrichment (no permanent wedge). One LOW note (not a finding): a throwing enrichment yields no client frame (unguarded await in the ws listener) — unreachable in practice (classifier never throws) and byte-identical to the already-shipped live SUBMIT_PROMPT shape; recorded as a 2D-2 note (client-side nonce timeout).
- files changed: 10 (+564/-0, purely additive): commands.ts, client-command.json, client-command.preview-route.json (regenerated), NEW preview.ts, server.ts (case added), authz.ts, NEW tests/preview-route.test.ts (9 tests), tests/authz.test.ts, tests/contracts-fixtures.test.ts. Byte-identical: enrich.ts, events.ts, sessions.ts, classifier.ts, dispatch.ts, router/engine.ts. Zero apps/console changes.
- next ticket: TCLAW-2D-2 — composer UI + read-only preview rendering (consumes the frozen frame shape from 2D-1; owes RC-1 busy guard + RC-2 verbatim caveat + nonce-timeout note).
- blockers: operator merge approval. CI-green on PR is the post-push gate.

### TCLAW-2D-2 — Route preview UI compose + read-only preview panel (Phase 1, Epic 2; completes the preview feature)

- date: 2026-07-10
- branch: ticket/tclaw-2d-2-preview-ui @ cb27a8e -> PR #17 (base master). NOT merged — awaiting operator approval. Second half of the 2D split; consumes the frozen 2D-1 PREVIEW_ROUTE frame. UI-only: 4 files (+679/-7): apps/console/src/components/TorqTerminal.tsx, apps/console/src/components/friendly.ts (one new pure selector selectLatestRoutePreview), NEW tests/torq-terminal-preview.test.tsx (18 tests), tests/friendly.test.ts (+6 selector tests). ZERO packages/* changes (preview.ts/commands.ts byte-identical to master).
- ticket: Epic 2 §9.2.4, CONSOLE RENDERING ONLY. Second half of the 2D split; owes the 2D-1 G1R mandates RC-1 busy guard + RC-2 verbatim caveat + nonce-timeout note.
- what shipped: "simulate route" button (controls bar, type=button, disabled on empty, fresh crypto.randomUUID() previewOf nonce PER CLICK); buildJudgment field-mapper factored from buildSubmit (preview and submit field-identical by construction; SUBMIT_PROMPT proven byte-identical via exact toEqual deep-equality on the REAL dispatched command across default/free/custom/numeric-budget states — RC-3); in-form preview panel with 5 honest states (pending/result/dropped/timeout 5s/sendFailed immediate); result renders solely via unchanged 2B helpers + honest fidelity label for KEYWORD_FALLBACK/DEFAULT (none for LOCAL_LLM); dropped in_flight = notice-and-done, structurally quarantined from the 2B formatters (never "no routing record"); nonce-keyed staleness (previewOf only, never prompt text; independent clears on input edit / control change / new click); RC-1 busy fix (busy computed over last NON-routePreview event; EMPTY REMAINDER -> busy=false — the fresh-console edge; scoped to routePreview ONLY with the TCLAW-UIFIX-1 comment); suppression guard += meta.routePreview; mandatory caveat exactly once in source and string-test pinned: "Preview only. Enrichment and route may change when you submit."; "would route" never "will route"; panel has zero interactive elements.
- G1R (Opus 4.7): APPROVE-WITH-CHANGES — 5 required folded (RC-1 empty-remainder busy edge + dedicated test; RC-2 named fake-timer steps 499/+4501ms act-wrapped; RC-3 byte-identical mapper assertion; RC-4 within(panel) zero-button scoping; RC-5 TCLAW-UIFIX-1 filed as tracked obligation) + 4 suggested folded (dropped-notice-not-no-routing-record pin; LOCAL_LLM no-caveat branch; independent staleness triple; selector naming convention). Rulings: OQ-a narrow routePreview-only busy fix contingent on the UIFIX-1 ticket; OQ-b in-form mount; OQ-c 5s timeout; OQ-d shared mapper in scope; dropped-frame notice-and-done (rollback rejected).
- G2A (Opus 4.8): PASS — zero findings. SEVEN empirical sabotages each caught by named assertions, reverted byte-identical: naive busy memo -> RC-1 test failed; dropped-through-result-branch (2 variants incl. a surgical formatRouteExplanation(null) leak) -> the "no routing record" pin failed independently; caveat minus one character -> exact-string pin failed; fixed nonce -> fresh-nonce test failed; stray key in buildJudgment -> all 4 byte-identity tests failed; button injected into panel -> within(panel) zero-button test failed. Vacuous-pass hunt clean (throwing panel anchor; before/after staleness assertions; genuine empty-remainder construction). All 4 Builder defect-fixes verified real (noUncheckedIndexedAccess guard; prev-value staleness comparison; sessionStorage.clear in afterEach; act-wrapped timers).
- tests/checks: full gate green (typecheck 12/12, vitest 613 = 589 pre-existing unmodified + 24 new, contracts:check, build 7/7, uv pytest 75). CI run 29129900971 = success @ cb27a8e (all stages + 6 e2e).
- files changed: 4 (+679/-7): apps/console/src/components/TorqTerminal.tsx, apps/console/src/components/friendly.ts, NEW tests/torq-terminal-preview.test.tsx (18 tests), tests/friendly.test.ts (+6 selector tests). Byte-identical: packages/* (preview.ts, commands.ts).
- status: awaiting operator merge approval. 2D COMPLETE pending merge (2D-1 backend merged at f9ca506; 2D-2 UI this PR).
- blockers: operator merge approval. CI-green on PR is the post-push gate.

### TCLAW-UIFIX-1 — Shared SYSTEM-frame predicates for busy/suppression (IMPLEMENTED — see completion entry below)

- filed: 2026-07-10, origin: TCLAW-2D-2 G1R review (RC-5) + G2A-confirmed pre-existing bug.
- scope: two predicates with a load-bearing subset relationship (isPanelSystemFrame ⟹ isBusyNeutralEvent, never the reverse). isBusyNeutralEvent(ev) = ev.type === 'SYSTEM', consumed ONLY by the busy scan — busy-truth rides on non-SYSTEM task events only. isPanelSystemFrame(ev) = SYSTEM && (routePreview || receiptList || receiptView || costSummary), consumed ONLY by the EventRow suppression guard — byte-identical coverage to today's guard. Memory frames and the Done receipt frame stay visible (busy-neutral but NOT panel frames); test matrix proving busy stays correct across all SYSTEM frame types, including the Done-receipt production order and the raw-relay unknown-metadata case.
- reason: G1R + G2A confirmed at code level that trailing SYSTEM frames flip the idle console into a spurious working…/stop state on master. The transient publishOnly panel frames (receiptList/receiptView/costSummary) are one class; memory SHOW/FORGET frames are PERSISTED via makeEmitter (seq-bearing, backlog-replayed user-requested output — NOT publishOnly) and must stay visible while being busy-neutral; and the SYSTEM 'Done' receipt frame (dispatch.ts, emitted after RESULT on every completed task) made the spurious flip fire on the happy path of every completion. 2D-2 deliberately fixed ONLY routePreview per scope discipline; this obligation tracks the general fix.
- status: IMPLEMENTED at commit 2e84928 (branch ticket/tclaw-uifix-1-busy-suppression, PR #18) — completion entry below.

### TCLAW-UIFIX-1 — Shared SYSTEM-frame predicates for busy/suppression (Phase 1, console-only bug fix)

- date: 2026-07-10
- branch: ticket/tclaw-uifix-1-busy-suppression @ 2e84928 -> PR #18 (base master). NOT merged — awaiting operator approval. Discharges the filed obligation above.
- ticket: console-only fix for the spurious working…/stop indicator. Root causes fixed: the SYSTEM 'Done' receipt frame (dispatch.ts:230-233, emitted after RESULT on EVERY completed task — the happy-path instance), the publishOnly panel frames (receiptList/receiptView/costSummary), memory SHOW/FORGET frames, and the markerless skill/approval confirmation frames no marker list could catch.
- what changed (5 files, +421/-15): apps/console/src/components/friendly.ts +2 pure predicates — isBusyNeutralEvent(ev)=ev.type==='SYSTEM' (busy scan only; G1R-verified invariant JSDoc'd in code: busy-truth rides on non-SYSTEM task events only, terminals emitted solely by dispatch.ts) and isPanelSystemFrame(ev)=SYSTEM && the 4 publishOnly panel markers (suppression only, byte-identical coverage to the inline check replaced); TorqTerminal.tsx — busy memo's ONLY code change is the skip condition -> isBusyNeutralEvent(e) (exclusion list + empty-remainder->false untouched; the inverted 2D-2 "do not broaden" comment rewritten per G1R RC-2), suppression guard -> isPanelSystemFrame(event), ReceiptCard branch untouched; tests/friendly.test.ts +6 (predicate units + subset pin with RC-4 NEGATIVES: memory-SHOW and Done-receipt are busy-neutral AND NOT panel); NEW tests/torq-terminal-busy-suppression.test.tsx (13: MANDATORY Done-receipt production-order test, per-type idle busy, memory-visible getByText, ReceiptCard-renders, RC-3 raw-relay pin [TIER_SELECTED,TOOL_CALL,SYSTEM{arbitrary}]->busy TRUE, mid-task memory->busy TRUE, table-driven empty-remainder over all 9 frame types, PENDING_APPROVAL regression pin); ledger RC-1 substantive correction (memory reclassified persisted-via-makeEmitter; Done-receipt named).
- G1R (Opus 4.7): APPROVE-WITH-CHANGES — 4 required folded (RC-1 substantive ledger correction; RC-2 invariant JSDoc + comment rewrite; RC-3 raw-relay unknown-metadata test; RC-4 subset negatives). Rulings: skip-all-SYSTEM adopted over the marker-conservative fallback (the only design covering the markerless flippers; verified exhaustively incl. the hermes.ts:136 raw-relay wildcard, neutralized by Invariant 7); two predicates with documented proper-subset relationship; extra producers in-ticket (free under skip-all); new dedicated test file. Scope-expansion ruled in-obligation because suppression coverage never widens — only busy-skip breadth.
- G2A (Opus 4.8): PASS — zero blocking findings (first run cut off by a session limit after corroborating the emitter classification; full rerun completed). THREE sabotages each fatal to the exact intended assertions, reverts SHA-confirmed byte-identical: (a) marker-based busy revert -> 9 assertions failed incl. the mandatory Done-receipt test; (b) memory added to the panel predicate -> memory-visible + subset NEGATIVES failed; (c) exclusion-list variant -> the RC-3 raw-relay pin + mid-task memory failed (the two discriminating pins). Vacuous hunt clean. Two non-blocking notes: the obligation header/status flip (fixed in this entry) and a cosmetic memoryForget fixture string divergence (harmless — no test asserts that text).
- tests/checks: typecheck 12/12; vitest 632 (613 pre-existing unmodified + 19 new); contracts:check 0; build 7/7; uv pytest 75. The 3 existing terminal test files (preview 18, route-chip 5, live-affordances 14) byte-identical to master and green.
- CI run on 2e84928 = success (all stages + 6 e2e).
- files changed: 5 (+421/-15). Zero packages/*; zero emitter changes; memory NOT demoted to publishOnly.
- next ticket: operator direction — remaining Epic 2 items (routing profiles, override stats) or other PRD priorities.
- blockers: operator merge approval. CI-green on PR is the post-push gate.

### TCLAW-FIX-G — Refresh receipt approval embeds after approval decision (FILED — follow-up obligation, not scheduled)

- filed: 2026-07-11, origin: TCLAW-5A scoping (frozen-embed discovery) + G1R deferral ruling (5A ships correct WITHOUT fix G; bundling a cross-task re-projection would blur 5A's "no approval-behavior change" charter).
- problem: run_receipts materialize at the PENDING_APPROVAL terminal (dispatch.ts safeMaterializeReceipt runs BEFORE any decision) and the blocked task is never re-projected afterward — decideApproval only updates tool_approvals; APPROVE re-mints a NEW task and REJECT creates another new degenerate task, so nothing ever touches the blocked task's receipt again. Every receipt's approvals[] embed is therefore frozen at 'pending' forever. Visible on master today: ReceiptsPanel renders "(historical — pending)" for long-decided approvals.
- scope when scheduled: re-project (or refresh) the blocked task's receipt after decideApproval — a WRITE-path change to the projection trigger, not a read surface. Must preserve: deterministic rebuildable projection (drop + rebuildAll reproduces byte-identical content), zero fabrication, invariant 7 (single terminal), and the guarded-wrapper discipline (a projector throw must never break the decide path).
- interim honesty: TCLAW-5A-1's LIST_APPROVALS reads the LIVE tool_approvals table precisely because the embed diverges. tests/approvals-read.test.ts test 9 pins the divergence in BOTH directions (live table 'approved' WHILE the same task's receipt embed still reads 'pending') — when this ticket lands, that test's frozen half flips and must be updated deliberately, not silently.
- status: FILED. Not scheduled — Phase-1 remainder (TCLAW-5A-2 UI + Card v2, TCLAW-5B safe export) comes first per operator direction.

### TCLAW-5A-1 — Approval history read surface + approval card gate facts (Phase 1, Visible Trust; first half of the 5A split)

- date: 2026-07-11
- branch: built on ticket/tclaw-5a-1-approval-read @ 43152df; RE-LANDED via ticket/tclaw-5a-1-reland (43152df cherry-picked onto post-revert master as new SHA 397e444, byte-identical across all 14 files) after the governance repair — see the re-land note below. Backend half of the G1R-mandated 5A split (5A-2 = history panel UI + Approval Card v2, only after this merges green).
- ticket: Phase-1 Visible Trust completion — let the operator read honest approval history without creating new approval powers or changing approval behavior.
- why a new read surface (the load-bearing finding, G1R independently confirmed): receipts CANNOT serve as the approval-status source — the embed is frozen at 'pending' forever (see the TCLAW-FIX-G filed entry above). LIST_APPROVALS reads the LIVE tool_approvals table via a tasks INNER JOIN for session ownership (fail-closed: an orphan approval with no tasks row is unlistable).
- what changed (14 files, +720/−6, one commit 43152df): commands.ts +LIST_APPROVALS (operator-only; limit 1-100 default 20; optional status enum pending|approved|rejected — 'expired' rejected at the contract boundary because no TTL exists; deliberately NO sessionId param); ClientCommand.json regenerated in BOTH checked-in dirs (byte-identical hunks, SHA-verified by G2A); approvals.ts +ApprovalSummary/listApprovals/handleListApprovals (SELECT + publishOnly only; summary columns ONLY — approvalId/requestId/toolName/status/createdAt/decidedAt, NO args per row [G1R RC-6], full args ride the existing GET_RECEIPT drill-down); dispatch.ts +buildGateFacts — additive `gate` key on the terminal PENDING_APPROVAL sourced from the SAME boot-time RegisteredTool entry that raised the block (no second classifyCapability call [RC-1]; registry miss carries NO capability — never 'read' [RC-2]; FRONTIER = 'engine-approval-hook' with no capability, since the engine hook has no capability classes and a same-named gateway entry would be a different tool; targets labeled targetsSource:'path-heuristic'); server.ts switch delegation (connection's own sid, never a client value); authz.ts explicit channel-deny case + rationale [RC-5]; bridge index one-line capability.js re-export; friendly.ts one-line approvalList marker in isPanelSystemFrame [RC-4, busy-neutral automatic under the UIFIX-1 invariant]; NEW tests/approvals-read.test.ts (16 tests incl. the REQUIRED two); authz/friendly/contracts-fixtures test pins.
- the two REQUIRED tests (operator-mandated): (1) DIVERGENCE — after a real registerApproval → materializeReceipt → decideApproval sequence, LIST_APPROVALS shows 'approved' + non-null decidedAt WHILE handleGetReceipt on the same task still embeds status 'pending' / decidedAt null — both halves asserted directly; (2) NO-FABRICATION — exact-key-set pin on the row shape, zero actor/decidedBy/expiresAt/ttl/expired anywhere, statuses ⊆ the three real states, decidedAt null-honest for pending.
- G1R (Opus 4.7): APPROVE-WITH-CHANGES + split — all 6 RCs folded (RC-1 same-registry-entry enrichment in 5A-1; RC-2 honest labels, miss never 'read'; RC-3 ReplayEventRow inertness is 5A-2's obligation; RC-4 approvalList marker; RC-5 explicit channel deny; RC-6 summary-only list). Rulings: tool-only v1 (gateway skill_queue is vestigial; the live engine queue has no timestamps/session linkage — unifying would fabricate); pending rows display-only; fix G deferred (filed above); partial PRD :426 card satisfaction shipped honestly over fabrication.
- G2A (Opus 4.8): PASS — zero blocking findings, 1 NIT (zero-write snapshot pins state-value equality, not literal write occurrence; a value-changing sneak IS caught — accepted). All 8 operator verification points confirmed EMPIRICALLY by sabotage, each failing the predicted named test: rerouting the list to the frozen run_receipts embed → divergence test failed; injected UPDATE → zero-write snapshot failed; args_json added → exact-key-set failed; channel ALLOW → authz pin failed; miss-defaulted-to-'read' → gate-facts miss test failed; marker removed → friendly pin failed. Anti-vacuousness audit clean (positive presence precedes every negative). Builder's own honest finding CONFIRMED: JOIN→LEFT JOIN alone is not what test 8 catches — the WHERE t.session_id clause is the actual fail-closed boundary (NULL never matches); a genuinely-listable orphan (LEFT JOIN + NULL-tolerant WHERE) DOES fail test 8, so the invariant is behaviorally pinned; no client influence over session binding exists.
- Builder (Sonnet 5) notes: executed the sabotage self-check live (7 scenarios) before handoff; surfaced the JOIN/WHERE imprecision honestly. Baseline pytest quirk documented: bare repo-root `uv run pytest` has a PRE-EXISTING collection error (reproduced on 33eeaef); the correct invocation is from engines/hermes_kernel (75/75 green).
- tests/checks (Builder-run, G1D independently re-verified, G2A re-verified): typecheck 12/12; vitest 652 (632 pre-existing unmodified + 20 new); contracts:check OK both dirs; build 7/7; uv pytest 75 (zero engine impact). CI: post-push gate on the re-land PR.
- incident (contained, no data loss): an unrelated parallel-workstream commit (graphify dual-graphs, operator-authored) landed on the ticket branch after the Builder finished, and PR #19 (graphify skill install) advanced master past the branch base. The foreign commit was preserved on chore/graphify-dual-graphs and removed from the ticket branch via MIXED reset (branch pointer only — zero on-disk file changes; content also intact in the preserved commit). No overlap between graphify files and the 14 ticket files.
- re-land note (2026-07-11, governance repair): the mixed 5A-1 + graphify work also reached master directly via the ungoverned merge f4ac4d8, forward-reverted by PR #21 (merge cf54a69, CI green). During the repair, PR #23 (opened from the original ticket branch) landed the ledger/FIX-G entry ONLY — the feature SHAs were already in master history, so its diff was empty of feature content (revert-a-merge trap). The originally cited "PR #20" was never opened (the number was consumed on the repo). This re-land PR re-lands the G2A-audited 5A-1 feature as the new commit 397e444 after the governance repair; no re-audit was required because the 14 files are byte-identical to the audited 43152df.
- next ticket: TCLAW-5A-2 — approval history panel UI + Approval Card v2 (consumes the frozen approvalList frame + gate facts; owes RC-3 ReplayEventRow inertness, the "write-class (unclassified)" render, the heuristic-targets label, the tool-only-v1 honesty sentence, and the rejected→"denied" display copy). Then TCLAW-5B safe export.
- blockers: operator merge approval. CI-green on PR is the post-push gate.

### TCLAW-5A-2 — Approval history panel UI + Approval Card v2 (Phase 1, Visible Trust; completes the 5A split)

- date: 2026-07-11
- branch: ticket/tclaw-5a-2-approval-ui @ 067d6b9 (single Builder commit, off master 5fb8485). NOT pushed, NO PR — awaiting operator approval. Console-only second half of the 5A split; consumes the 5A-1 backend frozen on master (LIST_APPROVALS, approvalList frame, PENDING_APPROVAL gate facts).
- ticket: operator can inspect this session's tool-approval history (live-table truth, read-only) and see mechanical gate facts on the live approval card. Zero new approval powers; zero approval-behavior change.
- what changed (exactly 7 files, +1345/−5): NEW apps/console/src/components/ApprovalHistoryPanel.tsx (+241 — CostPanel-skeleton overlay; snapshot-on-present null=loading/[]=empty; fixed {action:'LIST_APPROVALS', limit:20} on mount/refresh/staleness — no filter/limit UI, load-bearing for session-bus last-wins soundness; S1-S7 states; honesty footer "Tool approvals only — skill approvals are not shown in this history." / "Read-only. Pending requests are decided from their card in the terminal, not from this list." / "Statuses are as of the last refresh."; "Showing the 20 most recent." caption anchored to the backend ORDER BY DESC); MOD TorqTerminal.tsx (+96/−5 — approvals toggle + third overlay sibling, no mutual-exclusion logic; ToolPermissionCard widened with additive gate block; call site passes gate={meta.gate} + monotonic decidedCount primitive for the RC-4 no-loop staleness re-query); MOD friendly.ts (+226 — pure helpers selectLatestApprovalList / formatApprovalStatus / formatGateFacts / formatApprovalTimestamp + plain-data ApprovalHistoryRowData); NEW tests/approvals-panel.test.tsx (+344, P1-P16); MOD tests/torq-terminal-live-affordances.test.tsx (+206, describe F card variants F1-F11); MOD tests/friendly.test.ts (+212, H1-H4); MOD tests/torq-terminal-busy-suppression.test.tsx (+25/−4, approvalList table rows — additions only).
- honesty partition shipped (G1R RCs folded): absent gate → today's card byte-identical (never "unclassified" — pre-5A-1 frames had no registry lookup to report); malformed gate (null/primitive) → treated as ABSENT, never miss, never a card crash [RC-1]; registry miss → class row EXACTLY "write-class (unclassified)", never 'read', no rule/server rows; FRONTIER engine-approval-hook → engine line, NO capability word, wins over miss even on adversarial dual-signal; hits render capability verbatim (incl. genuine 'read') + mechanical rule line. Targets: Array.isArray guard [RC-2], "none detected" on empty/malformed, ≤4 inline w/ middle-truncate, "show all (N)" expander only >4 and dispatches nothing [RC-5 w/ ===4/===5 boundary fixtures], heuristic caption keyed on targetsSource==='path-heuristic'; unknown source → "targets source: <raw>" never the heuristic sentence [RC-6]. Status map exact-match only: rejected→"denied", unknown raw → verbatim neutral. Timestamps: SQLite strings verbatim + " UTC" on regex shape-match only — never new Date() (local-time lie), never relative. decidedAt null → segment omitted. No actor/TTL/expired anywhere.
- G1R (Opus 4.7): APPROVE-WITH-CHANGES, single ticket upheld — all 6 RCs + 4 SCs folded (RC-1 non-object gate guard; RC-2 targets guard; RC-3 invariant rescoped to the session bus — publishOnly reaches all tabs, so any future filter UI is unsafe until frames echo the query; RC-4 decidedCount primitive seam + wire-the-map-must-fail pin; RC-5 expander boundary ruling; RC-6 targetsSource fallback pin). All 11 scope questions ruled; obligations 10/10 covered.
- G2A (Opus 4.8): PASS — zero findings above NIT (one non-blocking LF/CRLF checkout advisory, zero content impact). SIXTEEN sabotages, each failing the predicted named test, all restored byte-identical: panel button wired to APPROVE_TOOL → P5 allowlist failed (no new approval powers); button injected into a PENDING history row → P4 failed with anti-vacuous presence-first confirmed (no hidden action path); marker/type guards dropped → H1 failed (no skill-history synthesis; skill queueId events yield zero rows); unknown status default-mapped → P10+H2 failed; gate ?? {} → F1/F1b failed; miss defaulted to 'read' → F4+H3 failed; frontier keyed on !capability → F5+H3 failed (the miss/frontier tooth); heuristic caption on unknown source → F6+H3 failed; expander off-by-one → F8 failed; targets guard removed → F6+H3 crash-failed; gate threaded into APPROVE_TOOL → F9 + two pre-existing 5A-1 pins failed (payload purity); staleness effect made render-keyed → P13 RC-4 seam pin failed (no-loop); toLocaleTimeString swap → P14+H4 failed. Vacuous-pass hunt clean. Anti-regression: extended test files additive-only, no assertion weakened.
- zero-confirmations (G2A-verified, byte-level): packages/* diff EMPTY (gateway/contracts/bridge untouched — 5A-1 backend behavior byte-identical; tests/approvals-read.test.ts untouched and green); graphify grep of full diff = zero hits (hard firewall held); no new approval powers (panel dispatch surface = LIST_APPROVALS only; decideApproval unreachable); APPROVE_TOOL payload byte-unchanged ({action, approvalId, decision} — not.toHaveProperty('gate')/('targets') pinned on the actual mock call arg; post-decision remnant carries no gate facts).
- tests/checks (Builder-run ×2, G1D independently re-verified, G2A re-verified post-restore): typecheck 12/12; vitest 712 (652 pre-existing unmodified + 60 new, 28 files); contracts:check OK both dirs untouched; build 7/7; uv pytest 75/75 from engines/hermes_kernel (zero engine impact; repo-root invocation retains the known pre-existing collection quirk). CI: post-push gate on the future PR.
- next ticket: TCLAW-5B (safe diagnostic export, server-side redaction) or 5A/Phase-1 closeout — ONLY after operator approval. TCLAW-FIX-G remains filed. TCLAW-GRAPHIFY-CLEANUP remains operator-lane.
- blockers: operator approval for push + PR; then merge gate; CI-green on PR is the post-push gate.

### TCLAW-FIX-H — Sanitize tasks.error at the persistence boundary (FILED — follow-up obligation, not scheduled)

- filed: 2026-07-11, origin: TCLAW-5B scoping (leak-surface inventory) + G1R RC-3 (independently confirmed in code).
- problem: dispatch.ts builds the failure reason unsanitized (`reason = String(error?.message ?? error)`) and persists it via taskStore.fail; sanitize() (Bearer-only regex + 2000-char cap) is applied ONLY to the emitted ERROR event message, never to the persisted value. A Bearer token (or key-bearing URL/provider host) in a provider error sits in tasks.error AND full_receipt_json.error in the DB at rest today.
- scope when scheduled: apply a scrub at the fail()/persistence boundary (and consider re-projecting affected receipts); coordinate the pattern set with packages/gateway/src/export.ts SECRET_SHAPES so at-rest and egress redaction cannot drift apart.
- interim mitigation: TCLAW-5B-1's GET_SAFE_EXPORT scrubs `error` on egress, so the SAFE export is unaffected; the exposure is at-rest (DB file, backups) and in the full in-console receipt view (operator-only, same trust domain).
- status: FILED. Not scheduled — Phase-1 closeout (TCLAW-5B-2) first per operator direction.

### TCLAW-5B-1 — Safe diagnostic export: gateway redactor + GET_SAFE_EXPORT (Phase 1, final deliverable, backend half)

- date: 2026-07-11
- branch: ticket/tclaw-5b-1-safe-export @ 2d36c3f (single Builder commit, off master 41bd648). NOT pushed, NO PR — awaiting operator approval. Backend half of the G1R-confirmed 5B split (5B-2 = console affordance + Markdown + marker, only after this merges green).
- ticket: operator-only GET_SAFE_EXPORT {taskId} returns a server-redacted per-task diagnostic JSON. SECURITY-CRITICAL: allowlist projection first (fail-closed — unknown fields never export), pattern scrub second (recursive-decode on retained string residue only), cap after scrub. Zero UI. Zero writes. safe_export_json stays deliberately NULL (on-demand generation; the materialized-redaction permanent-leak argument, G1R-verified against the real at-rest error gap).
- what changed (exactly 14 files, +1695/−2): NEW packages/gateway/src/export.ts (+743 — REDACTOR_VERSION/EXPORT_VERSION=1; SECRET_SHAPES fixed-order: bearer-token, api-key sk-, github gh[pousr]_, AKIA, whole-PEM, jwt, ssn, FP-narrowed card, path×3→[REDACTED:path] for bare-drive/UNC/POSIX-home incl. /root/ and ~; recursive-decode scrubText; pure buildSafeExport(row, liveApprovals, version); selectLiveApprovalsForExport; fail-closed handleGetSafeExport with byte-exact minimal fallback); NEW tests/export-redaction.test.ts (+834, 41 tests E1-E22 series); commands.ts +GET_SAFE_EXPORT {taskId} (no sessionId/includeEvents/format); both generated ClientCommand.json re-emitted; server.ts delegation; authz.ts explicit channel deny + rationale (redacted ≠ safe-for-lower-trust; sessionId-as-correlator decision noted); receipts.ts + schema.sql COMMENT-ONLY (deliberate-NULL rationale); receipt-projection.test.ts title-only staleness fix; authz/fixture pins; docs ledger +TCLAW-FIX-H filing (above).
- honesty architecture shipped (all 6 G1R RCs + 4 SCs folded): omitted wholesale = prompt, assembledContext, event replay (raw args/results), args_json, memory text — the handler never traverses those sources; scrubbed residue = errorClass(verbatim stable prefix)+error(scrub-then-2000-cap), router reason strings, name-guarded tool fields (/^[A-Za-z0-9_.:-]{1,64}$/ else [REDACTED:unparsed-tool-entry]); LIVE approvals re-queried from tool_approvals at export time, never the frozen embed (E14-handler pins the divergence end-to-end); redactionReport = counts-only, spoof-proof (operations-performed, never output-scanning), notice verbatim "Known secret shapes removed. This export does not and cannot claim to contain no secrets."; determinism/idempotence/marker-fixed-point pinned; ownership no-existence-oracle; oversize all-or-marker.
- Builder (Sonnet 5) notes: mandated checkpoint honored (redactor + unit tests green, helper API recorded, before wiring); teeth self-check run LIVE (11 sabotages) and it caught TWO gaps in its own suite — cap-before-scrub unpinned at the real buildSafeExport composition, and the frozen-embed sabotage uncaught at the handler call site — closed with E10-integration and E14-handler before handoff, all sabotages re-run green.
- G2A (Opus 4.8): PASS-WITH-NOTES — 17 independent sabotages (superset of the Builder's), every merge-blocking invariant confirmed on VALUE-ABSENCE over the whole serialized frame (the vacuity hunt decisively proven: a weakened marker-presence-only helper let a real product leak pass; the strong helper failed it). All 14 operator verification points PASS incl. FIX-H filed-not-implemented (dispatch.ts byte-unchanged). Fixture safety confirmed (all planted secrets obviously fake). Findings, none merge-blocking: LOW-1 fieldsOmitted list not test-pinned (dropping an entry ships silently — actual omission behavior IS enforced by E2/E3/E4); LOW-2 /root/ path shape present in code but absent from E11's corpus; INFO E8 redundant for the decode step (E9 carries those teeth). Disposition: operator APPROVED the pre-PR cleanup — both LOW findings closed by test-only additions @ efb47bf (E19c fieldsOmitted exact-list pin; /root/ added to the E11 path corpus); zero product change.
- tests/checks (Builder ×2, G1D independently re-verified, G2A re-verified post-restore; full gate re-run after the efb47bf cleanup): typecheck 12/12; vitest 758 (712 pre-existing unmodified + 46 new, 29 files); contracts:check OK both dirs; build 7/7; uv pytest 75/75 from engines/hermes_kernel (zero engine impact). CI: post-push gate on the PR.
- next ticket: TCLAW-5B-2 (console affordance: ReceiptsPanel copy JSON/Markdown, redaction-report display, isPanelSystemFrame marker, COPY_DIAGNOSTIC honest dual-affordance) — closes Phase 1 per the G1R closeout ruling (binding AC :471 satisfied; "full local receipt" served by the in-console GET_RECEIPT view). Only after operator approval. FIX-G + FIX-H remain filed, non-blocking.
- blockers: operator approval for the pre-PR cleanup decision + push + PR; then merge gate; CI-green on PR is the post-push gate.

### TCLAW-5B-2 — Safe diagnostic export console affordance (Phase 1, FINAL ticket, console half)

- date: 2026-07-12
- branch: ticket/tclaw-5b-2-safe-export-ui @ 2cffb4d (two separable commits per the operator's mandatory-half-first rule: 1e2af9b = closeout-critical mandatory half, gate-green standalone; 2cffb4d = terminal chip, shipped under the G1R RC-5 proof rule). NOT pushed, NO PR — awaiting operator approval. Per the recorded G1R closeout ruling, this ticket merging green CLOSES PHASE 1 (closeout ledger entry gated on merge + master CI green + operator approval — deliberately NOT written yet).
- ticket: console-only consumer of the frozen 5B-1 backend. ReceiptsPanel Safe-export section (fetch on click via a narrow onGetSafeExport callback — never raw sendCommand into ReceiptDetail; four safeExportView frame variants distinguished by KEY PRESENCE: ready/not-found/too_large/export_failed; redaction report rendered ABOVE the copy buttons; copy JSON = the frame's metadata.safeExport BY REFERENCE stringified, first key torqclawSafeExport; copy Markdown = renderSafeExportMarkdown(safeExport)); friendly.ts pure helpers (selectSafeExportViewByTaskId keyed per-task selector w/ malformed-payload skip; escInline backslash-first; fenceBlock fence = max(3, longest-internal-run+1) — structurally unbreakable; renderSafeExportMarkdown with the documented line-start template invariant, notice at top AND in the report, top-level-only version stamps); safeExportView added to isPanelSystemFrame (busy-neutral automatic); legacy chip relabeled EXACTLY "copy raw diagnostic (local, unredacted)" with behavior byte-unchanged (pinned by exact old-payload regression test); NO raw fallback anywhere (the export_failed copy states the policy: "This panel never falls back to raw data.").
- what changed (exactly 7 console/test files, +1860/−26): ReceiptsPanel.tsx +324; TorqTerminal.tsx +228 (relabel + chip); friendly.ts +406; tests/receipts-panel.test.tsx +395; tests/friendly.test.ts +340; tests/torq-terminal-live-affordances.test.tsx +168; tests/torq-terminal-busy-suppression.test.tsx +25. ZERO packages/*, engines/*, schemas, contracts, graphify.
- RC-5 chip proof (the make-or-demote gate): Builder PROVED the requestId↔taskId equivalence from the emission sites with a five-link citation chain (dispatch.ts:169 makeEmitter(req.id) → events.ts:87-91 requestId stamped on ERROR → dispatch.ts:185/events.ts:101 tasks.request_id = req.id → receipts.ts run_receipts.task_id = tasks.request_id → getReceipt/handleGetSafeExport key), embedded as a code comment; G2A independently re-verified ALL FIVE LINKS correct — the chip is legitimately shipped, not a guessing chip. Two-click fetch-then-copy (transient-activation-safe), Markdown-only, inert failure texts.
- G1R (Opus 4.7): APPROVE-WITH-CHANGES — all 6 RCs folded (RC-1 by-reference copy + first-key pin; RC-2 top-level stamps only, nested double-read dropped; RC-3 line-start invariant documented + expanded escaping corpus incl. all-backticks both paths and position-0 fence runs; RC-4 "not recorded" collision accepted + commented, no invented sentinel; RC-5 chip proof rule; RC-6 three-site atomic marker-suite update where the explicit expect line is the tooth) + SC-1 refreshing indicator, SC-2 why-in-corpus, SC-3 nested stamp dropped, SC-4 malformed-payload selector skip. Rulings: click-fetch (prefetch rejected — it trades the honesty gate for latency); two-click chip (one-click async rejected — WS-push writes are outside transient activation); textual not chromatic raw/safe distinction; report display ruled "the strongest part of the design".
- G2A (Opus 4.8): PASS — zero merge-blocking findings; one informational note (T10's panel-level last-wins assertion is weaker than the selector-level F2b pin, which robustly covers it — no action). First run cut off by an API session limit before any edits (tree verified clean); full fresh rerun completed. All 15 operator verification points + obligations A-G confirmed empirically by sabotage, each failing the predicted named test: JSON re-assembly/reshape → T5/T-L3; Markdown enrichment → T6; report moved below buttons → T4 DOM-order; raw-fallback button on export_failed → T7; relabel/payload tampering → T-L1/T-L2; marker dropped → F1's explicit expect; variant collapse → T8; fence-always-3 → F5+F4; escape-order swap → 6 tests incl. the order pin; notice hard-coding → T4 (varied-notice fixture); nested-stamp read → F3; clickable pending → T3; fabricated "copied" before resolve → T11; prefetch added → T2. Anti-regression clean (removals = title bumps + the deliberately-broken old selector, which IS the relabel tooth). Vacuous-pass hunt clean (exact-payload clipboard assertions throughout).
- Builder (Sonnet 5) notes: mandatory-half-first honored (commit 1 gate-green standalone); two deviations reported transparently and both G1R/G2A-endorsed (a refresh affordance making the SC-1 behavior reachable, ApprovalHistoryPanel precedent; frame-gated rendering so a refresh never blanks a valid report); one self-caught test bug corrected (over-asserting #/- escaping beyond the audited set, with the template invariant documented as the safety rationale).
- tests/checks (Builder ×2, G1D independently re-verified, G2A re-verified post-restore): typecheck 12/12; vitest 805 (758 pre-existing + 47 new, 29 files); contracts:check OK untouched; build 7/7; uv pytest 75/75 from engines/hermes_kernel. CI: post-push gate on the PR.
- next: operator gate — push + PR + merge; THEN the Phase-1 closeout ledger entry (restating the G1R closeout ruling verbatim: multi-surface refactor satisfied-as-architecture; FIX-G, FIX-H, GRAPHIFY-CLEANUP as non-blocking residuals) after master CI is green and the operator approves.
- blockers: operator approval for push + PR; merge gate; CI-green on PR is the post-push gate.

## ═══ PHASE 1 — Visible Trust MVP — CLOSEOUT ═══

TrustOS program (PRD-TCLAW-TRUSTOS-001 rev 1.2).

- date: 2026-07-13
- PHASE STATUS: Phase 0 (Foundation) COMPLETE · Phase 1 (Visible Trust MVP) COMPLETE · Phase 2 (Governed Learning MVP) NOT STARTED (awaits explicit operator approval to scope).
- final Phase-1 master SHA: f5fbee774c3803108f0268fe3f9303b59ec9b55d (TCLAW-5B-2 squash merge).
- post-merge master CI: GREEN — PR #29, run 29262262717 (success). Suite at closeout: 805 TS tests / 29 files + 75 Python, all green on f5fbee7.

- G1R CLOSEOUT RULING (Opus 4.7, verbatim): "5B-2 merging green closes Phase 1. Console multi-surface refactor is ruled satisfied-as-architecture (independent overlay panels + shared snapshot/predicate discipline, extended by this ticket). TCLAW-FIX-G, TCLAW-FIX-H, and TCLAW-GRAPHIFY-CLEANUP are non-blocking residuals to be restated in the closeout ledger entry."

- SHIPPED-TICKET CHECKLIST (each ticket once; all merged to master via the governed pipeline, squash-only):
  Phase 0 — 0A–0F foundation (contracts/schema-drift gate, capability classifier 0C, path-scope P5, token hygiene, Python/circuit-breaker tests).
  Epic 1 (spend/cost) — 1A-core (cap gate + ledger), 1A-attr (cost provenance), 1B (Cost Control Center UI).
  Epic 2 (route transparency) — 2A (router diagnostics), 2B (route chip selectors), 2C (live current-task route chip), 2D-1 (PREVIEW_ROUTE backend), 2D-2 (route-preview composer UI). [2D split backend/UI]
  Visible Trust receipts/replay — 4A (run-receipt projection), 4B-1 (LIST_RECEIPTS/GET_RECEIPT read surface), 4B-2 (console receipts panel). [4B split backend/UI]
  Approval surfaces — 5A-1 (LIST_APPROVALS read surface + approval-card gate facts), 5A-2 (approval history panel + Approval Card v2 render). [5A split backend/UI; serves BOTH deliverables 4 and 5]
  Safe export — 5B-1 (gateway redactor + GET_SAFE_EXPORT), 5B-2 (console safe-export affordance + Markdown + honest dual-diagnostic). [5B split backend/UI]
  QA/infra — QA-1 (React component harness), QA-2 (live-affordance coverage), UIFIX-1 (shared SYSTEM-frame busy/suppression predicates).

- PRD PHASE-1 DELIVERABLE MAP (:850 → satisfying ticket(s)):
  1. Route explanation panel + preview — 2A, 2B, 2C, 2D-1, 2D-2. ✓
  2. Run receipt page + replay — 4A, 4B-1, 4B-2. ✓
  3. Cost receipt with enforcement status — 1A-core, 1A-attr. ✓
  4. Approval card v2 — 5A-1 (gate facts), 5A-2 (Card v2 render). ✓
  5. Approval history API/UI — 5A-1 (LIST_APPROVALS), 5A-2 (history panel). ✓
  6. Basic Cost Control Center — 1B. ✓
  7. Safe diagnostic export (server-side redaction) — 5B-1 (redactor + command), 5B-2 (console affordance). ✓
  8. Console multi-surface refactor — SATISFIED-AS-ARCHITECTURE per the G1R ruling above (independent Receipts/Cost/ApprovalHistory/SafeExport overlay consumers sharing the keyed snapshot-on-present discipline + the UIFIX-1 isPanelSystemFrame/isBusyNeutralEvent predicates; QA-1/QA-2 harness; triple-stack coexistence smoke). No residual filed.

- ACCEPTANCE CRITERIA (PRD :852), all GREEN: every task has a structured route explanation (2A–2D ✓); every terminal task has a queryable receipt (4A ✓); approval card shows tool/args/category/targets/reason (5A-2 ✓ — HONESTY CAVEAT: tool ✓ and args ✓ are real; category/targets are heuristic/mechanical where applicable (category = registry-hit capability class or "write-class (unclassified)" on a miss, never fabricated; targets = path-heuristic over proposed args, labeled); reason = the mechanical gate rule only, never an invented risk assessment); cost state visible for cloud tasks (1B ✓).

- NON-BLOCKING RESIDUALS carried past closeout (none is a Phase-1 acceptance criterion; no hidden Phase-1 blocker remains):
  - TCLAW-FIX-G — receipt approval embed frozen at 'pending'; the live surfaces (LIST_APPROVALS, GET_SAFE_EXPORT) already read the live tool_approvals table, so operator-facing status is never stale. Filed, not scheduled.
  - TCLAW-FIX-H — tasks.error / full_receipt_json.error persisted unsanitized at rest; GET_SAFE_EXPORT scrubs `error` on egress, so the safe export is unaffected; exposure is at-rest only. Filed, not scheduled.
  - TCLAW-GRAPHIFY-CLEANUP — broken TORQCLAW-local graphify hook. Operator-lane; filed, not scheduled.
  - graphify-on-master — graphify tooling files (graphify.toml, scripts/test_graphify_smoke.py, .graphifyignore, .claude/skills/graphify, docs/graphify-remediation-report.md) landed on master via GOVERNED Graphify PRs #26 and #28; accepted as current repository state. Any relocation to pilotwaffle/Torq-graphify remains an operator-lane item and is optional. Orthogonal to the TrustOS deliverables; NOT a Phase-1 TrustOS blocker.

- SAFE-EXPORT HONESTY (do not overclaim): the safe diagnostic export removes KNOWN SECRET SHAPES only via the gateway-side allowlist-projection + pattern scrub. It does not, and cannot, guarantee that no secrets remain (novel/unknown shapes can pass). The artifact and all UI copy state exactly this; "review before sharing" is the operator's standing responsibility.

- PROGRAM RECORD: Phase 1 delivered under the governed pipeline (Fable/Opus G1D scope → Opus 4.7 G1R design review → Sonnet 5 Builder → Opus 4.8 G2A empirical sabotage-verification → operator merge gate; feature-branch-per-ticket, squash-merge only, per-ticket ledger entry with independently verified facts).

- next: Phase 2 (Governed Learning MVP) — NOT STARTED. No scoping begins without explicit operator approval.
